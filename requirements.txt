# --- Core (cần cho scaffold + test, không cần GPU/API) ---
PyYAML>=6.0
pytest>=8.0
streamlit>=1.36

# --- Vector store (DEC-018: Qdrant Cloud) ---
# Cần từ Tuần 1 để smoke test kết nối (scripts/smoke_qdrant.py). Client HTTP thuần,
# không tải model — không vi phạm ràng buộc "không gọi model thật khi import/test".
qdrant-client>=1.9

# --- Gate 0 (scripts/gate0_data_check.py) — chạy trên Colab/Kaggle, CPU đủ ---
# Không thuộc core scaffold; cài riêng khi chạy cổng dữ liệu:
#   pip install datasets scikit-learn pandas numpy
# datasets>=2.19
# scikit-learn>=1.3
# pandas>=2.0
# numpy>=1.24

# --- Tuần 2: embed + index (DEC-021) — CHỈ cài trên máy có GPU (Kaggle T4) ---
# KHÔNG thuộc core scaffold: kéo theo torch ~2.5GB. `src/retrieval/embedder.py`
# import FlagEmbedding BÊN TRONG hàm nên test local vẫn chạy khi thiếu gói này.
#   pip install "FlagEmbedding>=1.2,<2.0"
# FlagEmbedding>=1.2,<2.0        # bge-m3 dense+sparse trong 1 lượt forward
#
# TUẦN 3 CẦN LẠI GÓI NÀY, nhưng KHÔNG cần GPU: chỉ encode 1 query + rerank
# ~20 cặp mỗi truy vấn, CPU thừa sức (khác Tuần 2 phải embed 4.972 chunk).
# Tuần 7 HF Spaces CPU cũng chạy đúng đường này.
#
# sentence-transformers KHÔNG dùng cho embedder: nó chỉ ra dense, mà
# retrieval.hybrid=true cần sparse.
#
# RERANKER Tuần 3 KHÔNG cần thêm gói nào — DEC-037. `FlagEmbedding` gọi
# `tokenizer.prepare_for_model()`, API đã bị gỡ ở transformers 5.x nên nhánh
# reranker của nó CHẾT; `src/retrieval/reranker.py` vì thế đi thẳng
# `transformers.AutoModelForSequenceClassification` (torch + transformers đã
# theo sẵn embedder) và tự áp sigmoid trong repo.
#   FlagEmbedding  -> CHỈ còn dùng cho bge-m3 (nhánh sparse).

# --- Tuần 4: generation (DEC-045) ---
# `google-genai` (SDK hiện hành, 2.x), KHÔNG phải `google-generativeai` —
# gói đó đóng băng ở 0.8.6 và là SDK legacy. Kiểm 2026-09-07:
#   google-genai 2.22.0 (phát hành liên tục) · google-generativeai 0.8.6 (dừng)
#
# Nhẹ: `pip install --dry-run google-genai` chỉ thêm 5 gói (google-auth, distro,
# pyasn1, pyasn1_modules) — httpx/pydantic đã theo qdrant-client. KHÔNG kéo torch.
#
# ⚠️ TEST KHÔNG CẦN GÓI NÀY. `src/generation/generator.py` import SDK bên trong
# hàm và nhận `transport` tiêm được, nên 149/149 test chạy sạch trên clone chưa
# cài nó. Gói ở đây là để CHẠY app + deploy HF Spaces Tuần 7.
google-genai>=2.0

# --- GATED: chỉ dùng sau khi Gate 0 GO (Tuần 3+) ---
# Bỏ comment khi thực sự cần, để scaffold cài nhẹ và test nhanh.
# sentence-transformers>=3.0     # LOẠI ở Tuần 3 — DEC-033 + DEC-037. Đừng mở lại:
                               # reranker đi thẳng transformers, xem khối Tuần 2 trên.
# datasets>=2.19               # tải HF dataset (loader)
# ragas==0.4.3                  # đánh giá (Tuần 6). PIN CỨNG, KHÔNG dùng `>=`.
#
#   Verify 2026-08-19: ragas KHÔNG có bản >= 1.0 (88 release đều 0.x, mới nhất
#   0.4.3). `pip install --dry-run ragas==0.4.3` resolve sạch trên Python 3.13.2
#   + numpy 2.4.4 + datasets 4.8.4 + pydantic 2.13.4 → 33 gói.
#
#   TÊN TRƯỜNG (SingleTurnSample) — chép ở đây để soạn test set khỏi mở lại docs.
#   Cảnh báo cũ "0.1.x vs 0.2+" đã LỖI THỜI: 0.4.x dùng họ tên mới, KHÔNG phải
#   question / answer / contexts / ground_truth.
#     user_input             str        câu hỏi
#     reference              str        đáp án vàng                  } chỉ
#     reference_contexts     list[str]  VĂN BẢN chunk đáng lẽ lấy được } nhóm
#     reference_context_ids  list[str]  ID chunk đáng lẽ lấy được    } E
#     retrieved_contexts     list[str]  chunk retriever trả về (Tuần 3)
#     response               str        câu trả lời hệ thống sinh (Tuần 4)
#
#   `reference_contexts` + `reference_context_ids` PHẢI thu LÚC soạn nhóm E,
#   không thu bù sau được. Chúng mở khoá 3 metric KHÔNG cần LLM:
#     NonLLMContextRecall                  retrieved_contexts + reference_contexts
#     NonLLMContextPrecisionWithReference   (như trên)
#     IDBasedContextPrecision              retrieved_context_ids + reference_context_ids
#   Chỉ Faithfulness buộc có LLM — hoặc FaithfulnesswithHHEM (classifier T5 nhỏ,
#   không gọi API), tránh được bẫy "judge cùng họ Gemini với generator".
#
#   CONTRACT: ragas kéo theo 7 gói họ langchain + openai 2.x. Là dependency của
#   thư viện eval chạy OFFLINE sau pipeline, KHÔNG phải orchestration —
#   `RAGPipeline._route()` vẫn Python thuần. Xem DEC-022. GIỮ COMMENT tới Tuần 6.
# beautifulsoup4>=4.12          # strip HTML nâng cao (cleaner hiện dùng regex)
