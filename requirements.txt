# --- Core (cần cho scaffold + test, không cần GPU/API) ---
PyYAML>=6.0
pytest>=8.0
streamlit>=1.36

# --- Vector store (DEC-018: Qdrant Cloud) ---
# Cần từ Tuần 1 để smoke test kết nối (scripts/smoke_qdrant.py). Client HTTP thuần,
# không tải model — không vi phạm ràng buộc "không gọi model thật khi import/test".
qdrant-client>=1.9

# --- Gate 0 (scripts/gate0_data_check.py) — chạy trên Colab/Kaggle, CPU đủ ---
# Không thuộc core scaffold; cài riêng khi chạy cổng dữ liệu:
#   pip install datasets scikit-learn pandas numpy
# datasets>=2.19
# scikit-learn>=1.3
# pandas>=2.0
# numpy>=1.24

# --- Tuần 2: embed + index (DEC-021) — CHỈ cài trên máy có GPU (Kaggle T4) ---
# KHÔNG thuộc core scaffold: kéo theo torch ~2.5GB. `src/retrieval/embedder.py`
# import FlagEmbedding BÊN TRONG hàm nên test local vẫn chạy khi thiếu gói này.
#   pip install "FlagEmbedding>=1.2,<2.0"
# FlagEmbedding>=1.2,<2.0        # bge-m3 dense+sparse trong 1 lượt forward
#
# TUẦN 3 CẦN LẠI GÓI NÀY, nhưng KHÔNG cần GPU: chỉ encode 1 query + rerank
# ~20 cặp mỗi truy vấn, CPU thừa sức (khác Tuần 2 phải embed 4.972 chunk).
# Tuần 7 HF Spaces CPU cũng chạy đúng đường này.
#
# sentence-transformers KHÔNG dùng cho embedder: nó chỉ ra dense, mà
# retrieval.hybrid=true cần sparse.
#
# RERANKER Tuần 3 KHÔNG cần thêm gói nào — DEC-037. `FlagEmbedding` gọi
# `tokenizer.prepare_for_model()`, API đã bị gỡ ở transformers 5.x nên nhánh
# reranker của nó CHẾT; `src/retrieval/reranker.py` vì thế đi thẳng
# `transformers.AutoModelForSequenceClassification` (torch + transformers đã
# theo sẵn embedder) và tự áp sigmoid trong repo.
#   FlagEmbedding  -> CHỈ còn dùng cho bge-m3 (nhánh sparse).

# --- Tuần 4: generation (DEC-045, DEC-054) ---
# OpenRouter là API kiểu OpenAI -> chỉ cần một lời POST JSON, dùng httpx thẳng
# thay vì SDK `openai`. httpx vốn đã theo qdrant-client, nhưng KHAI BÁO RÕ ở đây:
# dựa vào dependency bắc cầu là mời một bản nâng cấp qdrant-client làm gãy
# đường sinh câu trả lời.
httpx>=0.27

# `google-genai` (SDK hiện hành, 2.x), KHÔNG phải `google-generativeai` —
# gói đó đóng băng ở 0.8.6 và là SDK legacy. Kiểm 2026-09-07:
#   google-genai 2.22.0 (phát hành liên tục) · google-generativeai 0.8.6 (dừng)
#
# Nhẹ: `pip install --dry-run google-genai` chỉ thêm 5 gói (google-auth, distro,
# pyasn1, pyasn1_modules) — httpx/pydantic đã theo qdrant-client. KHÔNG kéo torch.
#
# ⚠️ TEST KHÔNG CẦN GÓI NÀY. `src/generation/generator.py` import SDK bên trong
# hàm và nhận `transport` tiêm được, nên 149/149 test chạy sạch trên clone chưa
# cài nó. Gói ở đây là để CHẠY app + deploy HF Spaces Tuần 7.
# CHỈ cần khi `models.llm_provider: google` (đường dự phòng, DEC-054).
# Đang dùng OpenRouter nên gói này KHÔNG bắt buộc; giữ để đảo quyết định
# mà không phải viết lại code.
google-genai>=2.0

# --- Tuần 6: biểu đồ risk–coverage (DEC-063) ---
matplotlib==3.10.8
#
# KHÔNG để trong khối GATED như `ragas`, và đây là lựa chọn có lý do: claim
# chính của đề tài ("hệ thống biết khi nào KHÔNG đủ căn cứ để trả lời") được
# `contracts/constraints.md` định nghĩa là đo bằng **1 biểu đồ + 1 bảng**.
# Biểu đồ đó là `docs/risk-coverage.png` — nó phải dựng lại được trên máy sạch,
# không phải sau một bước bỏ comment mà người clone repo không biết là có.
#
# PIN CỨNG, KHÔNG dùng `>=`: cùng lý do đã ghi cho `ragas` — biểu đồ đem đi bảo
# vệ phải dựng lại ra đúng cái đã xem, và matplotlib đổi mặc định style giữa
# các minor version.
#
# Dùng ở ĐÚNG MỘT chỗ: `scripts/build_risk_coverage.py:draw()`, sau một
# `try: import matplotlib`. Thiếu gói thì script vẫn sinh đủ Markdown, chỉ bỏ
# biểu đồ và nói ra. `src/eval/risk_coverage.py` **không** import nó (thuần
# số) nên không có đường nào để test kéo matplotlib vào — ràng buộc #2.
#
# Backend ép `Agg` TRƯỚC khi import `pyplot`: máy dựng không có backend GUI, và
# để mặc định thì `pyplot` đi dò backend rồi hỏng theo kiểu khó đọc trên
# Windows headless.

# --- GATED: chỉ dùng sau khi Gate 0 GO (Tuần 3+) ---
# Bỏ comment khi thực sự cần, để scaffold cài nhẹ và test nhanh.
# sentence-transformers>=3.0     # LOẠI ở Tuần 3 — DEC-033 + DEC-037. Đừng mở lại:
                               # reranker đi thẳng transformers, xem khối Tuần 2 trên.
# datasets>=2.19               # tải HF dataset (loader)
# ⚠️ `ragas` ĐÃ CHUYỂN SANG `requirements-eval.txt` — 2026-09-11, Tuần 6 việc (3).
#   ĐỪNG bỏ comment ở đây, đừng chép lại dòng `ragas==` vào file này.
#
#   Lý do tách: đo lại bằng `pip install --dry-run` thì ragas kéo **35 gói**,
#   trong đó có **langgraph 1.2.11** — đúng thư viện ràng buộc cứng #1 gọi tên.
#   DEC-022 vẫn đúng (eval offline ≠ orchestration), nhưng nó được quyết khi con
#   số là "7 gói" và không ai nhắc langgraph. Giữ quyết định, đổi cách đóng gói:
#   core install (app + test + HF Spaces Tuần 7) không kéo langgraph nữa.
#
#   Và ragas 0.4.3 **cài được nhưng không chạy được** với dep mặc định (nó import
#   `langchain_community.chat_models.vertexai`, API đã bị gỡ ở 0.4.x) — phải pin
#   lùi `langchain-community<0.4`. Chi tiết trong `requirements-eval.txt`.
#
#   Verify 2026-08-19: ragas KHÔNG có bản >= 1.0 (88 release đều 0.x, mới nhất
#   0.4.3). Kiểm lại 2026-09-11: vẫn 0.4.3 là mới nhất, chưa có bản vá.
#
#   TÊN TRƯỜNG (SingleTurnSample) — chép ở đây để soạn test set khỏi mở lại docs.
#   Cảnh báo cũ "0.1.x vs 0.2+" đã LỖI THỜI: 0.4.x dùng họ tên mới, KHÔNG phải
#   question / answer / contexts / ground_truth.
#     user_input             str        câu hỏi
#     reference              str        đáp án vàng                  } chỉ
#     reference_contexts     list[str]  VĂN BẢN chunk đáng lẽ lấy được } nhóm
#     reference_context_ids  list[str]  ID chunk đáng lẽ lấy được    } E
#     retrieved_contexts     list[str]  chunk retriever trả về (Tuần 3)
#     response               str        câu trả lời hệ thống sinh (Tuần 4)
#
#   `reference_contexts` + `reference_context_ids` PHẢI thu LÚC soạn nhóm E,
#   không thu bù sau được. Chúng mở khoá 3 metric KHÔNG cần LLM:
#     NonLLMContextRecall                  retrieved_contexts + reference_contexts
#     NonLLMContextPrecisionWithReference   (như trên)
#     IDBasedContextPrecision              retrieved_context_ids + reference_context_ids
#   ⚠️ 3 METRIC TRÊN CUỐI CÙNG **KHÔNG** LẤY TỪ RAGAS — chúng trùng chức năng
#   với `src/eval/retrieval_metrics.py` đã có sẵn và đã được test phủ. Kéo thư
#   viện về để tính lại thứ repo đã tính là đúng cái trôi repo đã phải gỡ bốn
#   lần (DEC-044 · DEC-045 · DEC-046 · stats.py). Chỉ **Faithfulness** đến từ
#   ragas — nó là thứ repo thật sự không có (tách claim + NLI từng claim).
#
#   ⛔ `FaithfulnesswithHHEM` ĐÃ BỊ LOẠI, đừng mở lại: HHEM là cross-encoder
#   huấn luyện trên **tiếng Anh**, đem chấm tiếng Việt y khoa thì con số không
#   bảo vệ được. Bẫy "judge cùng họ Gemini" là thật, nhưng nhỏ hơn bẫy "judge
#   không hiểu ngôn ngữ đang chấm". Judge đang dùng: `openai/gpt-5` qua
#   **OpenRouter** — khác họ với generator, thêm 0 hạ tầng (DEC-054 trả sẵn).
#
#   CONTRACT: ragas là dependency của thư viện eval chạy OFFLINE sau pipeline,
#   KHÔNG phải orchestration — `RAGPipeline._route()` vẫn Python thuần. Xem
#   DEC-022. (Con số "7 gói" ở bản trước đã SAI: thực tế 35 gói, có langgraph.)
# beautifulsoup4>=4.12          # strip HTML nâng cao (cleaner hiện dùng regex)
