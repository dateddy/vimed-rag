# Handoff — Session 11 (2026-09-07 → 08)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở
> `DECISIONS.md`, state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**TUẦN 4 ĐÓNG** — T4.1 policy gate · T4.2 `GeminiGenerator` · T4.3 `LLMRewriter`
· T4.4 baseline · smoke end-to-end. **Vòng corrective hết mắt Fake.** Kèm: dọn 3
nợ Tầng 1, hiệu chỉnh ngưỡng bằng LOOCV rồi đổi vào config, chuyển cổng LLM sang
OpenRouter, **đo xong phương án B**.

- **DEC-044 … DEC-055** (12 dòng). DEC-051 supersedes điều kiện "tập giữ lại" của
  DEC-039; DEC-055 chuyển DEC-029 từ "chưa đo" → **đã đo**.
- **207/207 test PASS**, không chạm mạng. `constraints.md`: 3 mục Limitations đã
  sửa, **claim chính phải thêm điều kiện** — xem ⛔ dưới.

## ⛔ Kết quả đổi hướng đề tài

1. **Đổi văn phong: coverage 17/21 → 10/21** (81% → 48%), p = 0,0015 (DEC-055).
   **5/7 câu mất là lỗi THANG ĐIỂM** — bài vàng vẫn top-5, E-04/E-06 vẫn **hạng 1
   ở cả hai** văn phong, chỉ điểm tin cậy sụp.
   → Claim *"biết khi nào không đủ căn cứ"* **buộc kèm** *"khi câu hỏi ở văn phong
   sách giáo khoa"*.
2. **Trần coverage 81% là trần của THANG ĐIỂM, không phải truy hồi** (DEC-051).
   Tăng recall vô ích; phải **đổi tín hiệu tin cậy**. Hai phát hiện đi từ hai
   đường khác hẳn nhau và trùng khớp.
3. **Ngưỡng đã chốt + đã đổi**: `correct 0.933` / `incorrect 0.919` (DEC-052).
   Nhóm A giờ **ABSTAIN thật**. Bẫy đã tránh: dải AMBIGUOUS phải nằm **TRÊN**
   ranh giới, vì AMBIGUOUS vẫn trả lời.

## Blocker

- **Không có blocker chặn build.** Hạn mức Google 20 lượt/NGÀY đã gỡ bằng
  OpenRouter (DEC-054) — đổi lại phải **theo dõi credit** trước bảo vệ.
  Cluster Qdrant ngủ sau ~2 tuần; lần dùng cuối **2026-09-08**.

## Việc kế tiếp

1. **⚠️ COMMIT + PUSH — TOÀN BỘ PHIÊN CHƯA COMMIT.** 17 file dirty (`src/llm.py`,
   `config.yaml`, 2 script mới, 2 test mới, DECISIONS, STATUS, constraints).
   `origin/main` ở `0f00902`. **Việc rủi ro nhất hiện nay.**
2. **Soi tay 21 biến thể giọng bệnh nhân** (~15 phút) —
   `data/processed/testset_e_patient_candidates.md`, đặt `eyeballed: true`.
   Số của DEC-055 là **sơ bộ** cho tới khi soi xong.
3. **Xuất `trace` + baseline 59 câu** — đang dở, **chưa có script**. Gộp 3 việc
   vào một lượt: trace JSONL · leakage sau rewrite · baseline LLM-only.
   ⚠️ Lưu **max_logit của CẢ HAI lượt truy hồi**, không chỉ `action` cuối — thiếu
   thì không chia lại fold được, phải chạy lại ~30 phút.
   ⚠️ Đọc `result.retrieved`, **KHÔNG** đọc `result.chunks` (DEC-049).

## Việc treo ngoài code

- **Leakage SAU REWRITE chưa đo.** LOOCV chấm trên lượt truy hồi **đầu**; câu A/B
  còn một lần thử thứ hai chưa hiệu chỉnh. Đừng cho rằng 0/30 tự động còn đúng.
- **Nhánh `ANSWER_WITH_CAUTION` chưa từng chạy trên dữ liệu thật** — dải
  `[0.919, 0.933)` rỗng vì phân bố nhóm E lưỡng cực.
- **Chi phí Tuần 7 dùng số ĐO**: truy hồi 27s (không phải 17,2s dự tính), và
  **nhánh TỪ CHỐI đắt hơn nhánh trả lời** (60,0s vs 36,6s, do 2 lượt truy hồi).
- ⚠️ **Đừng dùng heredoc bash sửa chuỗi Python có `\n`** — hỏng 3 lần trong phiên;
  `tests/test_scripts_parse.py` thêm vào vì `pytest` không chạm `scripts/`.
- **E2 evidence-highlighting coi như chết** — giá đã trả cho phương án B (DEC-015).
