# Handoff — Session 03 (2026-08-07)

> ⛔ **MỘT PHẦN ĐÃ HẾT HIỆU LỰC (2026-08-08, DEC-013).** Dự án chuyển sang **1 người**.
> Mọi chỗ dưới đây nhắc "Member B" / `STATUS-A.md` / `STATUS-B.md` là **lịch sử, đừng làm theo**:
> việc kế tiếp #3 ("Ping Member B") không còn tồn tại, state đã gộp về `../state/STATUS.md`.
> Phần Gate 0 + bẫy kỹ thuật vẫn đúng nguyên. Trạng thái hiện hành: `../state/STATUS.md`.

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Owner phiên: Member A (Đạt).

## Đã xong trong phiên

- **Gate 0 chạy → `GO`.** Chạy 2 lần: gốc, rồi lại sau khi siết keyword vì số đổi
  (eval 3,423 → 2,645 QA). Số liệu + 3 giới hạn: `../contracts/gates.md`.
- Sửa CONFIG `scripts/gate0_data_check.py`: `CORPUS_SPLIT` `"train"` (**không tồn tại**)
  → `vinmec_article_content`; placeholder `"ViMedAQA"` → `tmnam20/ViMedAQA`.
- Siết `config/specialties.yaml` → **23 + 8 keyword**; sync `_FALLBACK_SPECIALTY_KEYWORDS`
  trong gate script (lệch nhau = chạy standalone ra số khác).
- Cứu 3 script audit vào `scripts/`: `policy_audit.py` · `kw_audit.py` ·
  `alignment_audit.py` → mọi số trong DEC-010…012 tái lập bằng 1 lệnh.
- Đóng 3 `TODO(unknown)` từ Session 2. DEC-007…012 append; ghi đè STATUS-A. 20 test xanh.

## Blocker

- **Không còn.** Gate 0 = GO → wire được `loader → embedder → indexer → retriever`.

## Việc kế tiếp

1. **CHỐT CHÍNH SÁCH GÁN KHOA trước khi viết `loader.py`.** Bảng 4 phương án ở
   `../state/STATUS-A.md`. Cốt lõi: siết keyword **không** chữa được trùng khoa (38.7%
   vẫn khớp cả 2) — lỗi ở quy tắc "keyword có mặt ở đâu cũng tính" áp lên bài ~7.8KB,
   không ở từ vựng. Khuyến nghị "TITLE hoặc ≥8 lần" (trùng 7.7%, ~4,100 bài, alignment
   vẫn PASS). Tái lập: `python scripts/policy_audit.py`.
2. Wire `loader.py` thật (bỏ `NotImplementedError` GATED) → `data/processed/`.
   Đóng DoD Tuần 1: ≥150 bài/khoa + thống kê theo khoa.
3. Ping Member B: test set v1 + rubric abstention A–E.

## Việc treo ngoài code

- **Xác nhận claim với GVHD** — treo từ Session 2; DEC-001…006 vẫn chưa được duyệt.
- **`STATUS-B.md` trống hoàn toàn.** Test set v1 + rubric A–E là đường găng Tuần 5–6;
  plan đã cố ý đẩy về Tuần 2 để tránh peak, nhưng chưa có ghi nhận nào là đã bắt đầu.
- Sync bản `.html` của plan — treo từ Session 2.

## Bẫy đã gặp — đừng mất thời gian lại

- Corpus là dataset **gated**: tải ẩn danh trả HTTP 401. Cần `HF_TOKEN` + accept terms
  bằng đúng tài khoản của token. Đã set **User scope**; `setx` không áp cho terminal
  đang mở → mở terminal mới. (Lần tải 2026-08-01 fail âm thầm đúng vì lý do này.)
  Đã cache ở `~/.cache/huggingface` (~465MB) — chạy lại không tải lại.
- **Đổi `specialties.yaml` → PHẢI chạy lại Gate 0** (DEC-012). Verdict cũ đo trên từ
  vựng cũ nên hết hiệu lực. Lần này eval tụt 778 QA mà suýt không ai để ý.
- **`solo=0` trong `kw_audit.py` có HAI nguyên nhân**, đừng xoá mù: bị cụm khác nuốt về
  TỪ VỰNG (thừa thật) vs trùng hợp của corpus (`sốc tim` — vẫn cần cho policy TITLE).
  Script tự phân biệt: đọc nhãn, đừng đọc số.
- `pytest` trần **crash lúc collect** (plugin `dash` global kéo `flask`, thiếu `click`):
  chạy `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q` → 20 passed.

## Trạng thái repo — CHƯA COMMIT

```
M .claude/settings.json  (harness tự thêm allowlist, có path scratchpad → xoá trước commit)
M brain/00-INDEX.md          M brain/contracts/gates.md    M brain/decisions/DECISIONS.md
M brain/state/STATUS-A.md    M config/specialties.yaml     M scripts/gate0_data_check.py
?? brain/handoff/2026-08-07-session03.md   ?? scripts/{alignment,kw,policy}_audit.py
```