# Handoff — Session 18 (2026-09-15)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở `DECISIONS.md`,
> state + mọi con số ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng — 🎉 TUẦN 7 ĐÓNG 4/4

**DEC-073 · DEC-074 · DEC-075** · **399 → 431 test PASS** · đã push hết.

- **DEC-073 — C3+B3.** Streamlit nay 3 tab, tab đầu chạy `RAGPipeline` đầu-cuối thật.
  Lỗi "5 nguồn cạnh câu từ chối" vá ở `app/display.py` (thuần, 22 test khoá **hai chiều**),
  **không** đụng pipeline → số Tuần 6 giữ nguyên. Vá kèm **byline lọt màn hình**. Bất biến
  hiển thị nay nằm trong `contracts/corrective-loop.md`.
- **DEC-074 — Space public chạy hệ đầy đủ.** sha `97d0fcaf4789`, `GEMINI_API_KEY` đã set.
- **DEC-075 — latency + cost.** `docs/latency-cost.md` (sinh bằng `build_perf_report.py`).

**Số vận hành: đọc `STATUS.md` mục "TUẦN 7 ĐÓNG 4/4"** — ở đó có cả 4 điều phải nói kèm
mỗi lần trích, và danh sách số đã **hết hiệu lực**. Đừng trích từ file này.

## Bốn thứ đi ngược dự đoán — đọc trước khi tin plan cũ

1. **`origin` đã HỎNG, không phải "quên push".** URL GitHub bị ghi đè bằng URL HF Space
   dị dạng ở Session 17 → `git push` thất bại **2 session liền**, URL gốc không còn ở đâu
   trên máy. `DEPLOY.md` đẩy Space bằng `hf upload`, **không** bằng git — remote đó chưa
   bao giờ cần tồn tại. Kiểm nhanh mỗi lô: `git remote -v` phải ra `github.com`.
2. **STATUS tự mâu thuẫn — ISSUE-070 lần thứ tư.** Còn khai `correct_threshold = 0.6` và
   "chưa nối pipeline", sai từ DEC-052. Một chỗ nằm **trong bảng** — người lấy số cho báo
   cáo sẽ đọc ra "cấu hình đang chạy lọt lưới 33%". Đã vá 3 chỗ.
3. **Chi phí chạy lại lô chỉ ~$0,035** — không phải mối lo như các bản trước ghi.
4. **`export_runs.py --force` là đường SAI:** ghi **nối thêm**, `OUT` hard-code,
   `runs.jsonl` gitignore → 118 bản ghi trùng `id` trong file mọi bảng Tuần 6 ăn vào.
   Đường đúng: script riêng + file riêng (`bench_latency.py`), có chốt chặn từ chối ghi đè.

## Bẫy đo — 3 phép đo hỏng trong cùng phiên, quy tắc đã mechanize

Dụng cụ đo tự nó làm hỏng số: bật **Chromium mới mỗi mẫu** → page-ready phồng **17×**;
hiệu chuẩn nhịp máy **trước** khi nạp model → báo động giả 13/16 câu; tái dùng **một tab**
→ `wait_for_function` thấy chữ còn sót, `E-01` ra **0,0 s** thay vì ~20 s.

**Quy tắc:** mỗi phép đo phải có **một đối chứng rẻ chạy song song** — nhịp CPU ở local,
`curl /_stcore/health` ở Space. Mẫu bẩn bị **loại**, không trộn vào p50.

## Việc kế tiếp — Tuần 8

1. **Báo cáo kỹ thuật** — Limitations đã có sẵn trong `contracts/constraints.md`.
2. **Slides** ~12–15 + Q&A (*"một mình gán nhãn, lấy gì đảm bảo?"*).
3. **Video demo dự phòng — BẮT BUỘC** theo (b), quay khi Space còn sống. Rồi để PRO hết hạn.

## Việc treo ngoài code

- ⚠️ **RÀNG BUỘC LỊCH CỨNG:** bảo vệ **23–27/9**, PRO hết **1/10 không gia hạn** → biên
  **4–8 ngày**. Hết PRO = Space Docker trên `cpu-basic` dừng = **link public trong DoD chết**.
- ⚠️ **Space ngủ sau 48 giờ**, không persistent storage → người mở đầu tiên trả **24 s
  container + ~28 s nạp model**. **Đụng vào Space trước buổi bảo vệ.**
- ⚠️ **Space public + khoá LLM = ai có link cũng tiêu credit** (phương án (b), đã đặt trần).
  Kiểm trước bảo vệ: `GET https://openrouter.ai/api/v1/key`.
- ⚠️ Đẩy lại `app/` phải đẩy **cả thư mục** (thiếu `display.py` = `ModuleNotFoundError`).
  `--exclude "**/__pycache__/**"` bị Git Bash bung glob → `rm -rf app/__pycache__` rồi đẩy.
- Mục treo còn lại (`--pairable-only` help sai · `data/processed/` gitignore · `ragas` 0.4.3
  · khuôn JSONL · bản sao `stats.py` · giọng bệnh nhân là cận dưới) **ở `STATUS.md`**.
