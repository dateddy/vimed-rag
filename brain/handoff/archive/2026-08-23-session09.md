# Handoff — Session 09 (2026-08-23)

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Trạng thái hiện hành đầy đủ: `../state/STATUS.md`.

## Đã xong trong phiên

- **✅ SAFETY SUBSET ĐÓNG — 50/50 câu** (`A18/B12/D8/E12`) tại `data/testset.jsonl`.
  **24/24 tiêu chí nghiệm thu PASS**. Tái lập: `python scripts/build_testset.py`.
- **✅ Việc 1.4 ĐÓNG — 6/6 PASS.** Mechanize policy sớm (kéo từ Tuần 5 về):
  `config/abstention_policy.yaml` v1.1. Bắt nhóm D 8/8 · 0 khớp E/A/B · 0 ca ranh giới.
  Tái lập: `python scripts/check_policy_coverage.py`.
- **Đổi thứ tự có chủ đích:** làm safety subset TRƯỚC Tuần 3, vì nhóm E là tập con của
  test set v1 — thứ đang chặn đo retrieval.
- **DEC-022…032** (11 dòng mới). Đáng nhớ nhất: **DEC-024** policy gate (7→8 lớp) ·
  **DEC-027** nhóm E phải qua phép kiểm ngược · **DEC-029** giới hạn văn phong test set.
- **5 commit** (`16400a0` → `843ed64`), cây sạch, **50 test xanh**.

## Blocker

- **Không còn.** Tuần 3 đã hết vật cản.

## Việc kế tiếp

1. **`git push origin main`** — 7 commit đang treo, `origin/main` còn ở `57c2caa`.
2. **Tuần 3 — `HybridRetriever`**: dense+sparse Qdrant + rerank `bge-reranker-v2-m3`.
   Nhóm E mang sẵn `reference_context_ids` (cấp **BÀI**) → dùng luôn làm ground truth
   recall@k, chỉ cần map chunk → `doc_id` trước khi so.
   ⚠️ Nhớ 2 chốt **DEC-021**: vector có TÊN (`"dense"`/`"sparse"`); lọc khoa match payload
   **`specialties`** (list) chứ không `specialty` số ít.
3. Bỏ comment `sentence-transformers>=3.0` trong `requirements.txt` cho reranker.

## Việc treo ngoài code

- **7 COMMIT CHƯA PUSH.**
- **`KEEP_A`/`KEEP_B` khoá theo id ứng viên → KHÔNG rebuild được từ clone sạch**
  (`data/processed/*` gitignore, id đổi mỗi lần sinh lại). `testset.jsonl` đã commit nên
  deliverable an toàn; chỉ đường tái lập hỏng. ~20 phút để chuyển sang khoá theo chỉ mục
  ViMedAQA như `KEEP_E_IDX`. Cần trước khi bảo vệ nếu hội đồng hỏi tái lập.
- **Nhãn `specialty` nhóm A/B KHÔNG đáng tin** (`specialty_verified: false`, DEC-030).
  Tuần 6 chỉ tách abstention theo nhóm A/B/D/E — **tuyệt đối không tách theo khoa**.
- **ABSTAIN-do-retrieval vẫn mang chunk** (`pipeline.py:99`) → rủi ro UI Tuần 7.
- `config/prompts/abstain.txt` cần ≥3 biến thể (policy thường / cấp cứu / hết căn cứ).
- **Phương án B (đo lệch văn phong) HOÃN** — DEC-029 có sẵn điều kiện kích hoạt,
  đừng nghĩ lại từ đầu.
- ~~Rotate key Qdrant~~ — **BỎ (DEC-032).** Đừng mở lại; rủi ro tồn dư ghi trong DEC.
- Chưa mở lại Streamlit bằng mắt (treo từ Session 5).
- Thống kê theo khoa chưa có artifact trong repo.

## Bẫy đã gặp

Đã chuyển thành tri thức bền trong `../state/STATUS.md`, mục **"Đã biết về test set —
đừng dựng lại"** (6 mục) và bổ sung 2 mục vào **"Đã biết về hạ tầng"** / **"về corpus"**.
Không lặp lại ở đây để tránh hai nguồn sự thật.

## Trạng thái repo

**Sạch.** 5 commit trong phiên, **không cái nào đã push**.
