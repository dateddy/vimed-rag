# Handoff — Session 05 (2026-08-09)

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Trạng thái hiện hành đầy đủ: `../state/STATUS.md`.

## Đã xong trong phiên

- **6 commit sạch**, scanner PASS cả 6. Ba commit dọn tồn Session 4 (`7a2b09e` audit
  scripts · `e60347a` DEC-016 + gates · `968079d` state), ba commit mới cho loader
  (`59eda05` config · `7496c10` loader · `ce20d10` state).
- **`loader.py` wire thật** — bỏ `NotImplementedError` GATED, áp DEC-016. Chạy trên
  corpus thật (32.604 bài) ra **đúng số đã chốt**: 727 tim_mach · 698 tieu_duong ·
  trùng 15 = 1.1%. Đầu ra `data/processed/corpus.jsonl` (1.410 bài, 12,8 MB).
- **Khối `data` + `DataConfig`** trong config: ngưỡng 25 đọc từ config, có test khoá
  lại việc hard-code. `run_ingestion.py` chạy load → clean → tag → JSONL, dừng đúng
  ở ranh giới GATED kế tiếp (embed Tuần 2 / index Tuần 3).
- **38 test xanh** (20 cũ + 18 test loader mới, chạy trên row giả — không tải HF).
- `contract-guard`: **0 violation**, 2 note cố ý (`MIN_ARTICLES_PER_SPECIALTY = 150`
  là tiêu chí Gate 0; `_DEDUP_PREFIX = 200`).
- **DEC-017** — data contract của loader (xem `../decisions/DECISIONS.md`).

## Blocker

- **Không còn.** DoD Tuần 1 chỉ còn thiếu "Qdrant chạy" (`docker-compose up -d`).

## Việc kế tiếp

Xem `../state/STATUS.md` mục "3 việc kế tiếp": Qdrant chạy → Tuần 2 (embedder thật +
2 collection + test set v1) → hỏi GVHD.

## Việc treo ngoài code

- **Xác nhận claim với GVHD** — treo từ Session 2, gồm 3 việc: duyệt DEC-001…006, báo
  dự án còn 1 người, xin duyệt bỏ κ. **Thầy không chịu → DEC-014 phải đảo trước Tuần 5.**
- **Đọc phần format dataset của RAGAS TRƯỚC khi soạn test set v1.** Plan cảnh báo:
  test set sai format thì Tuần 6 phải làm lại. Đây là việc đọc duy nhất có deadline.
- Sync bản `.html` của plan — treo từ Session 2.
- `.claude/settings.json` vẫn dirty có chủ đích (26 dòng allowlist rác + `Bash(env)`
  in ra `HF_TOKEN`). Treo từ Session 4. Dọn hoặc bỏ qua, đừng commit nguyên trạng.
- Chưa mở lại Streamlit bằng mắt sau khi thêm khối `data` (phần rủi ro đã kiểm).

## Bẫy đã gặp — đừng mất thời gian lại

- **1.425 và 1.410 đều đúng.** 1.425 = 727 + 698 (cộng dồn, 15 bài trùng đếm 2 lần);
  1.410 = bài phân biệt. DEC-016 ghi kiểu cộng dồn. Đã suýt tưởng loader chạy sai.
- **Ngưỡng chính sách KHÔNG đặt được vào `specialties.yaml`** — `gate0_data_check.py`
  đọc **mọi key cấp 1 làm tên khoa**, thêm key vào đó sẽ sinh "khoa" giả và hỏng cả
  3 script audit. Đó là lý do nó nằm ở `config.yaml` khối `data` (DEC-017).
- **Thêm field bắt buộc vào `AppConfig` làm đỏ 5 test** ở `test_pipeline_route.py` —
  helper `_cfg()` dựng `AppConfig` bằng keyword args đầy đủ. Lần sau grep chỗ dựng
  `AppConfig` trực tiếp trước khi thêm field, đừng chờ pytest báo.
- **`corpus.jsonl` bị gitignore** → **không tự có trên Kaggle**. Tuần 2 chạy embedding
  trên Kaggle T4 thì phải upload nó thành Kaggle Dataset, hoặc chạy lại ingestion ở đó
  với `HF_TOKEN`. Quyết trước khi ngồi vào máy.
- **cp1252 cắn lại ở lệnh ad-hoc.** Script trong `scripts/` đã tự `reconfigure`, nhưng
  `python -c` / heredoc in tiếng Việt vẫn `UnicodeEncodeError`. Đặt `PYTHONIOENCODING=utf-8`
  cho mọi lệnh rời. Test cũng vậy: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q`.

## Trạng thái repo — SẠCH (38 test xanh)

```
M .claude/settings.json   (cố ý để ngoài — việc treo duy nhất)
```
