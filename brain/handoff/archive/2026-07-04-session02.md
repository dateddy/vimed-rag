> Archived. Nội dung đã tách vào brain/contracts + brain/decisions.
> Đọc file này chỉ khi cần lịch sử; trạng thái hiện tại ở brain/state/.

# COMPRESS — ViMed-RAG (Session 2: Agentic integration + Tuần 1 scaffold)

> File bàn giao cho Claude Code / session mới. Đọc file này là đủ nắm trạng thái, không cần đọc lại chat.

---

## TL;DR — ĐANG Ở ĐÂU

**Giai đoạn:** Tuần 1, Nhánh A (Member A — infra/scaffold).
**Vừa xong:** (1) tích hợp quyết định partial-agentic vào project plan; (2) sinh script Gate 0; (3) sinh prompt scaffold cho Claude Code.
**Chặn hiện tại:** **Gate 0 (data sufficiency) CHƯA CHẠY.** Scaffold + stub được phép làm song song, nhưng KHÔNG wire data/model thật cho tới khi Gate 0 = GO.
**Việc kế tiếp ngay:** chạy `gate0_data_check.py` → báo verdict → rẽ nhánh (xem mục NEXT STEPS).

---

## 1. Định danh dự án

- **Tên (mới):** Xây dựng hệ thống hỏi-đáp y tế tiếng Việt ứng dụng **Corrective RAG có trích dẫn nguồn và cơ chế từ chối có hiệu chỉnh (calibrated abstention)** nhằm kiểm soát hallucination.
- **EN:** ViMed-RAG: Safety-aware Corrective RAG with Calibrated Abstention for Trustworthy Vietnamese Medical QA.
- **Bối cảnh:** môn IT Project · nhóm 2 người · Tuần 0 + 8 tuần · phạm vi 2 khoa: **tim mạch + tiểu đường**.
- **Stack CHỐT (không đổi):** `BAAI/bge-m3` (dense+sparse) · Qdrant · `BAAI/bge-reranker-v2-m3` · `gemini-2.5-flash` · RAGAS · Streamlit · deploy HF Spaces.

---

## 2. Trục đóng góp (phần lõi trí tuệ — đừng làm loãng)

CRAG gốc: retrieval kém → fallback **web search** (nguồn không kiểm chứng, rủi ro trong y tế).
ViMed-RAG thay bằng **calibrated abstention**: dùng rerank score + trạng thái grader làm điểm tin cậy → dựng **đường cong risk–coverage** → chọn ngưỡng trả lời/từ chối **từ dữ liệu** (không phải hằng số 0.3 tùy tiện).

**Claim:** "hệ thống biết khi nào KHÔNG đủ căn cứ để trả lời" (đo được: 1 biểu đồ + 1 bảng). **KHÔNG** claim "an toàn lâm sàng".

**Giới hạn ghi rõ trong báo cáo:** chưa có reviewer y khoa; nhãn abstention theo khả năng truy xuất + policy, không theo đánh giá lâm sàng; RAG giảm chứ không diệt hallucination.

---

## 3. Quyết định đã chốt session này (kèm lý do nén)

| # | Quyết định | Lý do |
|---|-----------|-------|
| 1 | **Partial agentic**, giữ scope IN/OUT | Full agentic = tái hiện công trình 2023–24, phá tính lặp của eval |
| 2 | Framing = **calibrated abstention + risk–coverage** | Claim đo được thay vì claim khẳng định |
| 3 | **KHÔNG LangGraph** — Python thuần + `trace` list | 4 node + max_iter=1 → LangGraph là chi phí thuần (cold start, version churn) |
| 4 | Ablation chunk **256 vs 512**, dời về **Tuần 2** | Rẻ nhất, hay bị hỏi nhất; chạy lúc index gần free |
| 5 | Safety subset **50 câu nhãn THEO CẤU TRÚC** (A–E) | Không có bác sĩ; LLM-as-judge = vòng luẩn quẩn (judge cùng họ generator) |
| 6 | E2 evidence-highlighting = **buffer cắt được** | Đẹp portfolio nhưng không đóng góp claim |

**Safety subset 50 câu (nhãn kiểm chứng được, KHÔNG cần bác sĩ):**
- A. Ngoài corpus (15) → ABSTAIN, verify bằng `grep`
- B. Ngoài chuyên khoa (10) → ABSTAIN, verify bằng metadata `specialty`
- C. Thiếu thông tin (8) → ABSTAIN/CLARIFY, rubric
- D. Vi phạm policy — liều cá nhân hóa (7) → ABSTAIN, **policy tự viết**
- E. Control trả lời được (10) → ANSWER, ViMedAQA ground truth (BẮT BUỘC có, nếu không hệ-thống-từ-chối-tất-cả đạt điểm tuyệt đối)
- Nhóm C/D: 2 người gán nhãn độc lập → báo cáo **Cohen's κ**.

---

## 4. Corrective loop — CONTRACT (Claude Code implement chính xác)

```
GraderState    = CORRECT | AMBIGUOUS | INCORRECT
TerminalAction = ANSWER | ANSWER_WITH_CAUTION | ABSTAIN

answer(query):
    ctx   = retrieve(query)         # trace RETRIEVE
    state = grade(ctx)              # rerank score, KHÔNG gọi LLM; trace GRADE
    CORRECT   -> ANSWER
    AMBIGUOUS -> ANSWER_WITH_CAUTION
    INCORRECT -> for i in range(max_iter=1):
                   q2=rewrite(q,ctx); ctx=retrieve(q2); state=grade(ctx)
                   CORRECT/AMBIGUOUS -> generate
                 else -> ABSTAIN
```
- `grader.grade()` **thuần** (score + 2 ngưỡng từ config, không model).
- `max_iter=1` = đúng 1 lần rewrite, không loop vô hạn.
- `trace: list[TraceStep]` đủ để Tuần 6 tách metric theo nhánh + vẽ risk–coverage.

**Chống hallucination = defense-in-depth 7 lớp** (grounding → retrieval → grounded gen → ngưỡng calibrated → grader → rewrite → ABSTAIN). RAGAS faithfulness chỉ **ĐO**, không phải prevention.

---

## 5. Kiến trúc kỹ thuật đã chốt (cho scaffold)

- **Orchestration:** Python thuần, `RAGPipeline._route()`, state = dataclass, `trace` list. KHÔNG LangGraph/LangChain cho control flow.
- **Dependency injection:** pipeline nhận `retriever/generator/grader/rewriter` qua constructor → test bằng Fake, chạy bằng Real.
- **Config-driven:** ngưỡng/model/top-k/chunk/max_iter đọc từ `config/`. Không magic number. 2 khoa khai báo trong `config/specialties.yaml` (single source of truth).
- **Stub discipline:** thuần logic → implement đủ + test; phụ thuộc data/model/API → interface + Fake + real-skeleton `raise NotImplementedError("GATED: Tuần N")`.

---

## 6. Artifacts đã tạo (session này)

| File | Trạng thái | Ghi chú |
|------|-----------|---------|
| `ViMed-RAG-Project-Plan-Detailed.md` | ĐÃ cập nhật đầy đủ | 12 chỉnh sửa: tên, claim, scope, tech stack, Tuần 2/4/5/6/7/8, metrics, risks |
| `gate0_data_check.py` | Sẵn sàng chạy (unit-tested) | Chạy trên Kaggle/Colab. Đếm coverage 2 khoa + eval + alignment |
| `CLAUDE_CODE_PROMPT_scaffold.md` | Sẵn sàng dán vào Claude Code | Sinh scaffold + stub cho repo |

**CHƯA đồng bộ:** bản `.html` của plan (không có trong tay session này — cần user gửi để sync).

---

## 7. NEXT STEPS (theo thứ tự, có điểm rẽ)

**A. Song song NGAY (không phụ thuộc nhau):**
1. **[User/Colab] Chạy Gate 0.** Lượt 1 `INSPECT_ONLY=True` xem schema → sửa CONFIG (đặc biệt `EVAL_DATASET` đang là placeholder "ViMedAQA", phải điền path HF thật) → lượt 2 ra verdict.
2. **[Claude Code] Chạy prompt scaffold.** Dựng repo, `pytest` xanh, Streamlit demo chạy bằng Fake (không cần API key/Qdrant/GPU).

**B. Điểm rẽ theo verdict Gate 0:**
- **GO** (pass cả 3) → wire component thật: `loader.py` (data thật), `embedder.py` (bge-m3), `indexer.py` (Qdrant), `retriever.py` (hybrid+rerank). Plan giữ nguyên.
- **NO-GO #1** (corpus <150/khoa) → bù `hungnm/vietnamese-medical-qa` hoặc đổi/gộp khoa.
- **NO-GO #3** (alignment fail — đáp án không nằm trong corpus) → **NGHIÊM TRỌNG, DỪNG.** Quyết eval strategy (chỉ eval tập con answerable, hoặc đổi eval) TRƯỚC khi viết dòng build nào.

**C. Sau khi scaffold + Gate 0 GO → Tuần 2:** embed corpus, index Qdrant, ablation chunk 256 vs 512, front-load rubric safety subset.

---

## 8. Ràng buộc Claude Code PHẢI tôn trọng

- KHÔNG LangGraph, KHÔNG LangChain cho orchestration.
- KHÔNG gọi model/dataset thật khi import hoặc test (mọi thứ nặng phải nằm sau Fake/GATED).
- KHÔNG chạy `run_ingestion.py` với data thật cho tới khi Gate 0 = GO.
- Grader phải thuần (không LLM). max_iter=1 cứng.
- Ngưỡng grader để default tạm + comment "chọn từ risk-coverage Tuần 6".
- Nếu repo đã có file sẵn → đọc trước, đừng ghi đè.

---

## 9. Việc treo (user cần xử lý, ngoài code)

1. **Chạy Gate 0 + báo verdict** — chặn toàn bộ build data-dependent.
2. **Điền `EVAL_DATASET` path HF thật** trong `gate0_data_check.py` (đang là placeholder).
3. **Xác nhận claim với GVHD** — 5 phút, tránh đi sai hướng vài tuần. Claim này đo được nhưng **khiêm tốn về novelty** (CRAG đã có); nếu GVHD kỳ vọng novelty thuật toán → bàn lại hướng.
4. **Sync bản `.html`** của plan (gửi file để cập nhật).
5. Sau scaffold: chạy `pytest` + mở Streamlit xác nhận DoD trước khi wire real.

---

## 10. Bối cảnh nhóm

- 2 thành viên. Member A (Đạt) = infra/retrieval/pipeline. Member B = eval/generation/test set.
- Rủi ro tiến độ lớn nhất: **Tuần 5–6 peak của Member B**. Đã front-load rubric safety subset về Tuần 2. Thứ tự cắt nếu kẹt: E2 → ablation reranker; giữ risk-coverage + Static-vs-Corrective bằng mọi giá.
