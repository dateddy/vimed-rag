# Handoff — Session 07 (2026-08-13)

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Trạng thái hiện hành đầy đủ: `../state/STATUS.md`.

## Đã xong trong phiên

- **SOI CHUNK XONG → `DEC-020`.** Chốt giữ `size=512, overlap=50`; **không**
  snap-to-sentence, **không** sửa `cleaner.py`, **không** chạy lại ingestion.
  Cửa sổ "sửa chunking còn rẻ" đã đóng đúng cách — từ đây đổi chunking = embed lại
  cả 2 collection.
- **`scripts/chunk_audit.py`** (mới) — chấm tự động 4 cờ trên **toàn bộ** chunk + xuất
  N mẫu markdown kèm đường cắt để soi tay. Chỉ đọc `corpus.jsonl` local: không chạm
  model/mạng, **không cần `HF_TOKEN`**. Tái lập: `python scripts/chunk_audit.py --size 512`.
- **3 tiêu chí gốc đều PASS**, số đầy đủ trong DEC-020 + `../state/STATUS.md`. Đạt soi
  tay 25 mẫu (512): **0 chunk mất nghĩa** — chỉ "hơi thừa" (mẫu 1), "hơi cụt nhưng vẫn
  hiểu" (mẫu 9, 10).

## Blocker

- **Không còn.** Tuần 2 (embed + index) đã mở, không còn gì chặn.

## Việc kế tiếp

Xem `../state/STATUS.md` mục "3 việc kế tiếp": embedder thật + 2 collection →
đọc RAGAS + pin version → test set v1. Hai cảnh báo Kaggle (corpus không tự có ở đó ·
key phải qua Secrets) nằm sẵn trong mục đó, đọc trước khi mở notebook.

## Việc treo ngoài code

- **ROTATE API KEY QDRANT — VẪN CHƯA LÀM** (treo từ Session 6). Sau khi rotate phải
  verify **key cũ trả 401/403**, không chỉ verify key mới chạy được.
- **Đọc format RAGAS + pin version** — việc đọc duy nhất có deadline thật.
- **Chưa soi file 256 bằng mắt — đã đánh giá là KHÔNG cần.** DEC-004 index cả hai size
  nên "256 kém hơn" là *kết quả ablation*, không phải sự cố phải embed lại; còn quyết
  định đắt (snap-to-sentence) áp cho cả 2 size và đã trả lời xong bằng file 512.
- **Byline Vinmec có tên bác sĩ + bệnh viện** (đầu 17,6% bài, không lọc vì chỉ 0,16%
  token). **Tuần 4–5 làm citation phải để ý** đừng để hệ thống trích thành "BS X khẳng
  định…" — quy kết sai nguồn, khó biện minh cho hệ safety-aware.
- Chưa mở lại Streamlit bằng mắt (treo từ Session 5).
- Thống kê theo khoa chưa có artifact trong repo (~10 phút nếu cần).

## Bẫy đã gặp — đừng mất thời gian lại

- **Tỉ lệ cờ cao ≠ vấn đề thật.** "73% chunk cắt giữa câu" nhìn như thảm hoạ nhưng soi
  tay thì 0/25 mất nghĩa: `overlap=50` giữ **trọn** câu bị cắt ở chunk trước. Cờ tự
  động chỉ để khoanh vùng, đừng để nó tự quyết.
- **Đo trước khi sửa — cả 3 nghi ngờ đều chết vì số.** Bảng liều bị ép phẳng (nghi từ
  mẫu 4) → 0,1%. Byline → 0,16% token. Boilerplate footer → 0 chunk có >25%. Không cái
  nào đáng chạy lại ingestion. Nếu chỉ soi mẫu mà không đo toàn corpus thì đã sửa nhầm.
- **`data/processed/` bị gitignore** → 2 file `chunk-audit-*.md` **không commit được**.
  Số liệu chỉ sống sót nhờ đã chép vào DEC-020 + STATUS. Chạy lại script để tái tạo.
- **Session 6 ghi "working tree clean" nhưng thực tế không.** `00-INDEX.md` (M) và
  `handoff/2026-08-12-session06.md` (??) vẫn chưa commit tới đầu phiên này. Đừng tin
  dòng "trạng thái repo" của handoff trước — chạy `git status`.

## Trạng thái repo

**Dirty, chưa commit.** `M` 00-INDEX · DECISIONS · STATUS — `??` chunk_audit.py ·
handoff session06 · handoff session07 (file này).
Không đụng `src/` phiên này → 38 test không đổi (chưa chạy lại).
