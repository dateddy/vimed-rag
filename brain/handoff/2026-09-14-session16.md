# Handoff — Session 16 (2026-09-14)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở `DECISIONS.md`,
> state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**TUẦN 6 ĐÓNG 3/3** · **DEC-070** · **391 → 398 test PASS** · 3 commit `71aa8fe` `6201618` `54c47d5`.

- **ISSUE-069 đóng** — Đạt duyệt `concept_variants.jsonl` (30/30, **0 nhãn đổi** ngoài
  trường provenance). Bảng độ nhạy 14–16% nay trích được.
- **Lô chấm RAGAS xong** — corrective 17/17 · llm_only 16/16 · **blocker credit gỡ**.
- **Audit Tuần 6** → ISSUE-073/074/075, **vá cả 3** + 7 test mới.
  `01_OPEN_ISSUES.md` nay 7 issue, **0 còn mở**.
- **`brain/` viết lại** — STATUS **1006 → 474 dòng** (giữ nguyên **270 dòng** "đã biết,
  đừng làm lại"), DEC-070, INDEX, và **1 drift trong `constraints.md`**.

## Số đổi — bản trích cũ hết hiệu lực

Ghép cặp **9 → 16 câu** · corrective **0,722 → 0,747** · LLM-only **0,497 → 0,481** ·
Δ **+0,225 → +0,266** · **p = 0,0391 → 0,0042**. Hiệu ứng **mạnh lên**, mẫu gần gấp đôi.
⚠️ Chỗ còn ghi `n=9` / `p=0,0391` (S15, DECISIONS) là **lịch sử append-only** — đừng trích.

## Bốn thứ đi ngược dự đoán — đọc trước khi tin plan cũ

1. **Việc Đạt báo "đã duyệt" hôm trước thì CHƯA xảy ra trên đĩa.** Ba dấu độc lập mới
   thấy (`classified_by` · `git status` · mtime). **Xác minh trước khi lập kế hoạch dựa
   trên nó** — và **mtime là thứ truy ra được ai làm gì lúc nào** khi git chưa ghi nhận.
2. **Cả 3 lỗi audit cùng một hình dạng, và là lần thứ BA** (sau ISSUE-071): câu văn nối
   thẳng vào báo cáo thì **không phép kiểm nào chạm tới**, nên nó sống sót qua chính sự
   kiện làm nó sai. Chữa: tách ra **hàm thuần**, khoá **cả hai chiều**.
3. **Sửa ở GỐC không phủ được file VIẾT TAY.** DEC-064 sửa `fmt_pct()` nên mọi artifact
   *sinh ra* được đặt tên đúng, nhưng `constraints.md:110` vẫn ghi `< 11%` là *"quy tắc
   số ba"* (11% là cận **Wilson**; quy tắc số ba cho **10%**). Đã sửa.
4. **Agent làm bẩn `docs/ragas.md` ngay trong lúc review** — chạy script sinh doc mà
   thiếu `--pairable-only`, biến một lô **đã xong** thành khung "còn dở". Phục hồi bằng
   cờ đúng. Sai sót đó chính là thứ **phơi ra ISSUE-074**.

## Việc kế tiếp

1. **Commit `brain/` rồi `git push origin main`.** Đang treo **3 commit code + 5 file
   `brain/` chưa commit**, trong đó có **nhãn người không tái tạo được**. Làm trước mọi thứ.
2. **🔴 Smoke test HF Spaces đầu tuần — rủi ro demo số 1.** Rerank **75–82 s/truy vấn
   trên CPU**. Kèm: đụng cluster Qdrant cho tỉnh (ngủ lâu → bị xoá → **6,2h GPU** index
   lại), và **quyết dứt điểm rotate key Qdrant** — điều kiện đảo DEC-032 là *"trước khi
   deploy HF Spaces Tuần 7"*, tức bây giờ.
3. **C3 + B3 gộp — UI đầu-cuối.** Bắt buộc dùng `answer_content.is_refusal_text()` để ẩn
   khối nguồn ở câu từ chối. Rồi latency p50/p95 + cost/1.000 query
   (`export_runs.py --no-cache`; dùng số **đo** ~45s/27s, **không** dùng 17,2s của DEC-042).

## Việc treo ngoài code

- **`--pairable-only` help còn ghi `(48 câu -> 10)`** — số đúng **59 → 16**. Ngoài phạm
  vi vá, chưa sửa.
- **Lỗi hiển thị "5 nguồn cạnh câu từ chối"** (`A-01`/`E-21`) chưa vá — chỗ vá đúng là
  **lúc viết tab đầu-cuối C3**, không phải pipeline.
- Mục treo còn lại (`data/processed/` gitignore · `ragas` 0.4.3 không chạy được với
  `langchain-community` 0.4.x · khuôn JSONL `testset_e_patient` · đừng chép bản sao
  `stats.py` · giọng bệnh nhân là cận dưới · rủi ro tiến độ 1/2 năng lực) **ở `STATUS.md`**.
