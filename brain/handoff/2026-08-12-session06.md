# Handoff — Session 06 (2026-08-12)

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Trạng thái hiện hành đầy đủ: `../state/STATUS.md`.

## Đã xong trong phiên

- **DoD TUẦN 1 ĐÓNG** — cả 3 tiêu chí: repo chạy (38 test xanh) · 727/698 bài clean +
  tag · **Qdrant chạy** (Cloud `eu-west-1-0.aws`, smoke test OK, 0 collection).
- **DEC-018** — vector store = **Qdrant Cloud**, không Docker local. `QdrantConfig`
  thêm `api_key`; `docker-compose.yml` hạ xuống dự phòng offline (không xoá); thêm
  `scripts/smoke_qdrant.py` để tái kiểm kết nối bất cứ lúc nào (chỉ đọc).
- **DEC-019** — **GVHD duyệt DEC-001…006 + DEC-014.** Việc treo lớn nhất (mở từ
  Session 2) đã đóng. DEC-014 **không còn phải đảo trước Tuần 5**.
- Dọn `.claude/settings.json`: 42 → 22 rule (bỏ rule rò secret + rule trỏ scratchpad chết).
- **4 commit sạch**, scanner PASS cả 4: `4f71459` settings · `8bfb049` config Cloud ·
  `7580d59` smoke test · `6fcc382` brain. **Chưa push.**

## Blocker

- **Không còn.** Tuần 1 đóng, Tuần 2 mở.

## Việc kế tiếp

Xem `../state/STATUS.md` mục "3 việc kế tiếp": soi 20–30 chunk bằng mắt → embedder
thật + 2 collection → đọc RAGAS + pin version.

## Việc treo ngoài code

- **ROTATE API KEY QDRANT — CHƯA LÀM.** Key hiện tại (ID `170b4ef0`, quyền `m` =
  read-write toàn cluster) từng bị dán vào `.env.example`. Chưa commit nên **không có
  gì lên remote**, đã quét lại history sạch. Xoá key cũ → tạo mới → dán vào `.env`.
  Sau khi rotate phải verify **key cũ trả 401/403**, không chỉ verify key mới chạy được.
- **Đọc format dataset RAGAS + pin version** — việc đọc duy nhất có deadline thật.
  Chặn test set v1, mà test set v1 chặn đo retrieval Tuần 3.
- ~~Sync bản `.html` của plan~~ — **BỎ QUA theo quyết định của Đạt (2026-08-12).**
  Bản `.html` giờ lệch `.md` ở DEC-013…019. Đừng nêu lại.
- Chưa mở lại Streamlit bằng mắt (treo từ Session 5; phần rủi ro đã kiểm bằng code).
- Thống kê theo khoa chỉ in ra stdout, chưa có artifact trong repo (~10 phút nếu cần).

## Bẫy đã gặp — đừng mất thời gian lại

- **`.env.example` ĐƯỢC GIT TRACK.** Dán key thật vào đó là đẩy secret vào repo.
  Quy tắc: `.env.example` chỉ placeholder · giá trị thật chỉ ở `.env` (gitignore).
- **`QDRANT_URL` không phải URL trang dashboard**, phải là REST endpoint
  `https://<cluster-id>.<region>.cloud.qdrant.io`. **Dò region bằng DNS là vô ích** —
  Qdrant Cloud wildcard DNS nên mọi region đều resolve; probe 13 region không ra.
  Copy từ ô "Endpoint" trên trang cluster.
- **Handoff Session 5 ghi "chỉ còn `docker-compose up -d`" là SAI** — Docker chưa hề
  được cài trên máy. Đừng ghi "chỉ còn 1 lệnh" cho lệnh chưa từng chạy thử.
- **Here-string PowerShell `@'…'@` dùng trong Bash tool** → `@` lọt vào commit message.
  Bash phải dùng heredoc `git commit -F - <<'EOF'`. Đã amend (chưa push nên hợp lệ).
- **Xoá dòng đã comment trong `requirements.txt`** thay vì bỏ comment → package cài
  local nhưng không khai báo ở đâu, clone mới thiếu. Bỏ comment, đừng xoá dòng.
- **Skill `git-commit` có danh sách scope CỐ ĐỊNH** (`config` `index` `brain` `eval`…).
  `claude` / `scripts` không có trong đó — đừng bịa scope, bỏ trống còn hơn.

## Trạng thái repo

Working tree clean · 38/38 test xanh · 4 commit chưa push.
