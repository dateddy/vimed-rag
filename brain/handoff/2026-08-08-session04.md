# Handoff — Session 04 (2026-08-08)

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Trạng thái hiện hành đầy đủ: `../state/STATUS.md`.

## Đã xong trong phiên

- **Commit sạch 3 lần** (`52584fe` gate · `2e2a12e` solo · `a74119a` state). Toàn bộ việc
  Session 3 treo trong working tree đã vào git. Scanner PASS cả 3 lần.
- **Chuyển dự án sang 1 người** — DEC-013…015. Gộp `STATUS-A/B` → `STATUS.md`, bỏ vai
  Member A/B khỏi `CLAUDE.md` + plan, thay bằng 2 luồng L1 Hệ thống / L2 Đo lường.
  Kèm **bỏ Cohen's κ + bỏ nhóm C** của safety subset (DEC-014, supersedes DEC-005) —
  phương pháp thay thế ghi ở `../contracts/constraints.md`.
- **Chốt chính sách gán khoa** (DEC-016) — chặn cuối cùng trước `loader.py` đã gỡ. Gate 0
  tái kiểm dưới chính sách mới, cả 3 tiêu chí PASS; bảng ở `../contracts/gates.md`.
- Script mới `scripts/policy_cost_audit.py`; `alignment_audit.py` thêm dòng chính sách đã chốt.

## Blocker

- **Không còn.** `loader.py` được phép wire ngay.

## Việc kế tiếp

Xem `../state/STATUS.md` mục "3 việc kế tiếp": commit file dirty → wire `loader.py`
theo DEC-016 → hỏi GVHD.

## Việc treo ngoài code

- **Xác nhận claim với GVHD** — treo từ Session 2, giờ thêm 2 việc: báo dự án còn 1 người,
  và xin duyệt việc bỏ κ. Nếu thầy không chịu, DEC-014 phải đảo **trước Tuần 5**.
- Sync bản `.html` của plan — treo từ Session 2.
- `.claude/settings.json` dirty có chủ đích: 26 dòng allowlist rác trỏ scratchpad session
  đã chết + `Bash(env)` in ra `HF_TOKEN`. Dọn hoặc bỏ qua, đừng commit nguyên trạng.

## Bẫy đã gặp — đừng mất thời gian lại

- **`both%` (trùng khoa) là chỉ số đánh lừa** — đã suýt chốt sai chính sách vì nó. Nó bão
  hoà ~2% từ ngưỡng ≥15 trong khi 80% bài `tim_mach` vẫn lọt bằng đếm thân bài. Chỉ số
  dùng được là `title-hit`. Chi tiết + nhiễu còn lại: mục "Đã biết về corpus" trong STATUS.
- **Ràng buộc Qdrant Cloud free 1GB chưa từng được tính** trong 3 phiên đầu. Tuần 2 phải
  index HAI collection (chunk 256+512, DEC-004) → chính sách cũ ăn 89% free tier, tức là
  không deploy được, độc lập với chuyện tag đúng sai. Mọi quyết định đụng kích thước corpus
  từ giờ phải chạy `scripts/policy_cost_audit.py` phần 2.
- **Console Windows là cp1252**: `print` tiếng Việt trong script Python `UnicodeEncodeError`
  dù file đã ghi xong. Đặt `PYTHONIOENCODING=utf-8` (script trong `scripts/` đã có sẵn).
- **Rename + viết lại nội dung trong cùng 1 commit làm mất `--follow`.** `STATUS-A.md` →
  `STATUS.md` bị ghi thành D+A; nội dung cũ còn ở `074ae89`. Lần sau tách 2 commit.
- Corpus **gated** → cần `HF_TOKEN` (User scope, phải mở terminal mới); cache ~465MB ở
  `~/.cache/huggingface`. `pytest` trần crash lúc collect (plugin `dash`) →
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q`.

## Trạng thái repo — CHƯA COMMIT (20 test xanh, `src/` không bị đụng cả phiên)

```
M .claude/settings.json (cố ý để ngoài)   M brain/00-INDEX.md
M brain/contracts/gates.md   M brain/decisions/DECISIONS.md   M brain/state/STATUS.md
M scripts/alignment_audit.py   ?? scripts/policy_cost_audit.py   ?? handoff này
```
