# Handoff — Session 12 (2026-09-08)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở
> `DECISIONS.md`, state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**TUẦN 5 ĐÓNG** — nền dữ liệu Tuần 6 đã có. `data/processed/runs.jsonl`:
118 bản ghi = 59 câu × 2 hệ (corrective + baseline LLM-only), mang điểm **cả
hai** lượt truy hồi, hạng bài vàng từng lượt, cơ chế abstain, `retrieved`, chi phí.

- **DEC-056 · DEC-057 · DEC-058.** `constraints.md` sửa 2 chỗ (contract).
- **246/246 test PASS** (207 → 246), không test nào chạm mạng.
- **7 commit** — `002b114` … `87e4601`. Working tree sạch.

## ⛔ Kết quả đổi hướng đề tài

1. **Leakage THẬT = 2/30 = 7%, không phải 0/30** (DEC-056). Lượt 1 sạch 0/30
   (tái lập LOOCV), nhưng rewrite kéo `A-01` và `A-08` qua ngưỡng. Cơ chế đo
   được: Δlogit trung vị **+0,876**, kiểm định dấu 23 tăng/7 tụt, **p = 0,0052**.
2. **⚖️ Vòng corrective cứu 0/4 câu E, làm lọt 2/30 câu A/B** (DEC-057). Tác
   dụng đo được **duy nhất** trên test set này là **tạo ra leakage**. Claim
   *"vòng corrective làm hệ thống tốt lên"* — **trục đóng góp** — hiện không
   chống đỡ được. Vào **Chương kết quả**, không giấu ở Limitations.
   → Đường đo thứ ba cùng một hướng: DEC-051 · DEC-055 · DEC-057.
3. **Nhóm E hết sơ bộ** (DEC-058): soi tay đủ 21 câu, **0 loại 0 sửa**, chạy
   lại ra **đúng từng con số** của DEC-055.

## Blocker

- **Không có blocker chặn build.** Qdrant còn sống (dùng 2026-09-08).
- ⚠️ Theo dõi **credit OpenRouter** trước bảo vệ — hết tiền là 402.

## Việc kế tiếp

1. **⚠️ `git push origin main` — treo 12 commit.** Việc **rủi ro nhất**: mất máy
   = mất 3 kết quả đổi trục đề tài. `origin/main` còn ở `0f00902`.
2. **TUẦN 6 — chạy được ngay, không phải chạy lại pipeline.** Thứ tự DEC-015:
   (1) bảng **Static vs Corrective** — ⚠️ phải báo cáo cả kết quả **âm** DEC-057;
   (2) **risk–coverage** — `src/eval/risk_coverage.py` còn stub `NotImplementedError`;
   (3) **RAGAS** — `src/eval/run_ragas.py` cũng stub, và `ragas==0.4.3` vẫn
   **comment** trong `requirements.txt`, phải quyết cài trước.
3. **Dọn nợ hàm thống kê (~10 phút).** `src/eval/stats.py` là bản có thẩm quyền
   nhưng `calibrate_threshold.py` (wilson, rule_of_three) và
   `eval_register_shift.py` (sign_test) **vẫn giữ bản riêng**. Ba bản đang giống
   nhau; lệch = hai bảng trong CÙNG báo cáo dùng hai định nghĩa CI khác nhau.

## Việc treo ngoài code

- **`runs.jsonl` KHÔNG được commit** (`data/processed/*` gitignore, đúng thiết
  kế) → clone sạch **không có** nền dữ liệu Tuần 6 cho tới khi chạy lại
  `python scripts/export_runs.py` (~45 phút). Thứ commit là báo cáo trong `docs/`.
- **⚠️ Lỗi hiển thị chưa vá:** câu `ANSWER` hiện **5 nguồn** cạnh một câu nói
  "không có thông tin" (A-01, E-21 — DEC-056). **Coverage tính theo `action`
  đếm dư 1.** Vá ở tầng nào cũng là một quyết định, chưa chọn.
- **⚠️ `data/testset_e_patient.jsonl` phải giữ khuôn JSONL — mỗi bản ghi MỘT
  dòng.** Sửa tay lúc soi đã bẻ dòng một lần, `eval_register_shift.py:89` crash.
- **Chi phí Tuần 7 phải chạy `export_runs.py --no-cache`** — lô hiện tại có
  cache nên cột giờ/token vô nghĩa. Quan sát: **~45s từ chối · ~27s trả lời**.
- **E2 evidence-highlighting coi như chết** (DEC-015, giá trả cho phương án B).
