# Handoff — Session 15 (2026-09-11 → 2026-09-13)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở `DECISIONS.md`,
> state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**DEC-064…069** · **316 → 391 test PASS** · đã push `969fd7a`.

- **B1** — chốt **Wilson** toàn repo, gọi tên phương pháp mọi lần trích. Sửa ở **gốc**
  (`fmt_pct`) nên mọi call site được đặt tên cùng lúc. Thêm `tests/test_stats.py`.
- **A1 RAGAS — CODE XONG, LÔ CHẤM DỞ.** `run_ragas.py` hết stub · judge `openai/gpt-5`
  qua OpenRouter · `requirements-eval.txt` tách riêng.
- **B4** — coverage **hai tầng** (`action` 17/21 = số chính · `nội dung` 16/21 = độ nhạy),
  thêm `src/eval/answer_content.py`. Không đụng pipeline.
- **B5** — whitelist `calibration_scores.json` (8,5 KB): clone sạch dựng lại được
  `docs/risk-coverage.png`.
- **B6+B7** — không code, thành 2 mục Limitations.
- **B2 nửa đầu** — Đạt duyệt 30 nhãn `static_leak_review.jsonl`, **0 câu đổi**.
  **Con số 20% nay trích được.**
- **B3** — không phải "vá 30 phút" như plan cũ; xem mục dưới.

## Ba thứ đi ngược dự đoán — đọc trước khi tin plan cũ

1. **Faithfulness TRỪNG PHẠT câu từ chối đúng bằng `0,0`** (`E-01` trả lời thật = 0,70 ·
   `E-21` từ chối = 0,00). Phiên này dự đoán `1.0`/`nan` — **sai chiều**. Hệ quả cứng:
   **không bao giờ** lấy trung bình trên toàn bộ câu. Lý do đầy đủ: DEC-065.
2. **`ragas==0.4.3` cài được nhưng KHÔNG chạy được** với dep mặc định — pin cứng không
   chặn nổi vì hỏng nằm ở tầng **dưới** cái pin. Và nó kéo **35 gói có `langgraph`**
   (không phải "7 gói"). Xem `requirements-eval.txt`.
3. **Claim của chính B1 bị dữ liệu bác:** "Wilson bảo thủ hơn" chỉ đúng khi **`n ≥ 14`**.
   Kết luận vẫn đứng vì mọi mẫu số repo trích đều `n ≥ 19`. DEC-064.

## ⛔ Blocker — credit OpenRouter

`GET /api/v1/key` (2026-09-12): `limit $3` · `usage $3,17` · **`remaining 0`**.
Lô chấm chết ở **403** sau 28/76 lượt. **Chi phí đo được: ~$0,11/câu.**

- Nhánh `corrective_t1` **đã xong 17/17**. Còn **đúng 7 lượt** với `--pairable-only` (~$0,80).
- Đọc phần đã có, **0 lượt API**: `python scripts/build_ragas_report.py --cache-only`
- ⚠️ **Đừng đổi `--model` cho rẻ** — khoá cache gồm tên model nên đổi là **chấm lại cả 76**.

## Việc kế tiếp

1. **⚠️ VIỆC NGƯỜI, KHÔNG CHỜ CREDIT — đóng ISSUE-069.** `concept_variants.jsonl` vẫn
   **30/30 `classified_by: claude-draft-pass`**; nó nuôi **bảng độ nhạy 14–16%**, thứ
   chống đỡ cho DEC-062. Phiếu đã sinh sẵn (7,9 KB, toàn bảng):
   `--worksheet-variants` → đọc → `--approve --reviewer dat`.
   Hai bẫy ghi trong phiếu (đừng bỏ `A-17/A-18/B-06`, đừng gộp `variant_generic_of_brand`).
2. **Nâng hạn mức key → chạy `build_ragas_report.py --pairable-only`** → Tuần 6 đóng.
3. **Tuần 7 — C3 gộp cả B3.** UI chưa có tab đầu-cuối nào; khi viết phải dùng
   `answer_content.is_refusal_text()` để **ẩn khối nguồn** ở câu từ chối.
   ⚠️ Rủi ro demo số 1: **rerank 75–82s/truy vấn trên CPU** — thử HF Spaces **đầu tuần**.

## Việc treo ngoài code

- **`docs/ragas.md` đang mang số của lô DỞ** — báo cáo tự ghi rõ, nhưng đừng trích rời.
  Số đọc được: ghép cặp 9 câu, corrective **0,722** vs LLM-only **0,497**, **p = 0,0391**.
- **Streamlit đứng yên ở Tuần 3.** Đã chữa chỗ nó in *"đừng tin con số này, ngưỡng 0.6
  đã bị bác bỏ"* cho người xem — ngưỡng đó không còn tồn tại từ DEC-052.
- **Audit trail mới:** `01_OPEN_ISSUES.md` (ISSUE-069…072 + risk_accepted + rollback).
- Mục treo cũ (`data/processed/` gitignore · `export_runs --no-cache` · rotate key
  Qdrant trước HF Spaces · cluster Qdrant ngủ) **ở `STATUS.md`**.
