# Handoff — Session 08 (2026-08-18)

> Ảnh chụp trạng thái. Contract sống ở `../contracts/` — chỉ trỏ tới, không lặp lại.
> Trạng thái hiện hành đầy đủ: `../state/STATUS.md`.

## Đã xong trong phiên

- **TUẦN 2 ĐÓNG — hai collection đã sống trên Qdrant Cloud.** Chạy trên **Kaggle T4**.
  `verify_index.py` PASS cả 4 mục cho cả 2 size: `vimed_rag_512` = **4.972 point** ·
  `vimed_rag_256` = **10.246 point** (khớp DEC-020) · dense 1024/Cosine + sparse đều có ·
  chunk thuộc 2 khoa **67 / 140** → bất biến DEC-017 đứng vững trên dữ liệu thật.
- **`DEC-021` — hợp đồng dữ liệu lúc index.** `ChunkRecord` tách khỏi `RetrievedChunk`,
  point ID UUID5, payload lọc theo `specialties` (list), named vector `dense`/`sparse`,
  FlagEmbedding + `embed_hybrid()`, tham số index ra khối `index` trong config.
- **Code mới:** `BgeM3Embedder` · `QdrantIndexer` · `chunk_records()` · `IndexConfig` ·
  `build_index.py` · `verify_index.py` · 2 notebook. **50 test xanh** (38 cũ + 12 mới,
  không cái nào tải model hay mở kết nối).
- **Dung lượng thật (telemetry):** 97,7 MB ≈ **9,5%** free tier 1GB (DEC-016 dự toán ~8%
  → lệch +1,5 điểm, trong sai số, không cần DEC mới).

## Blocker

- **Không còn.** Tuần 3 (hybrid + rerank) đã hết vật cản hạ tầng.

## Việc kế tiếp

Xem `../state/STATUS.md` mục "3 việc kế tiếp": đọc format RAGAS + pin version →
test set v1 (~20 câu) → Tuần 3 `HybridRetriever`.

**Khi vào Tuần 3, nhớ 2 chốt của DEC-021** (sai là hỏng âm thầm): vector có TÊN —
`"dense"` / `"sparse"`; lọc khoa phải match payload **`specialties`** (list, có KEYWORD
index), KHÔNG phải `specialty` số ít.

## Việc treo ngoài code

- **2 COMMIT CHƯA PUSH** (`00d7a90`, `cab18e1`). `origin/main` đang ở `57c2caa`.
- **ROTATE API KEY QDRANT — VẪN CHƯA LÀM** (treo từ Session 6, sang phiên thứ 3).
  Sau khi rotate phải verify **key cũ trả 401/403**, không chỉ verify key mới chạy.
- **Đọc format RAGAS + pin version** — việc đọc duy nhất có deadline thật.
- **`notebooks/colab_build_index.ipynb` CHƯA TỪNG CHẠY** — dự phòng khi Kaggle hết quota
  GPU (30h/tuần). Đừng tin nó chạy được, phải soi lại cell secret + upload trước.
- Chưa mở lại Streamlit bằng mắt (treo từ Session 5).
- Thống kê theo khoa chưa có artifact trong repo (~10 phút nếu cần).

## Bẫy đã gặp — đừng mất thời gian lại

- **`RetrievedChunk` KHÔNG index được.** `specialty` số ít → 15 bài thuộc cả hai khoa chỉ
  vào được một khoa. Lỗi **im lặng**: số point vẫn đúng 4.972, chỉ filter khoa là thiếu
  bài. Đã tách `ChunkRecord` + test riêng. **Mục [3] của `verify_index.py` là chỗ DUY
  NHẤT bắt được lỗi này** — đừng bỏ qua khi index lại.
- **Kaggle: "Accelerator bị mờ" + "`git clone` fail" = MỘT nguyên nhân** — chưa xác minh
  SĐT, Kaggle khoá GPU *và* Internet cùng lúc. Xác minh xong là hết cả hai. Đừng debug
  hai triệu chứng riêng lẻ, và đừng vội đổi nền tảng (đã lỡ viết thừa 1 notebook Colab).
- **Máy local KHÔNG embed được, đã đo, đừng thử lại.** i5-1135G7 + Iris Xe (không CUDA):
  **0,22 chunk/s** → ~12–13 giờ cho cả hai size; thêm thread không cứu. Ước lượng ban đầu
  "1,5–4 giờ" sai thấp ~4 lần — **phải đo mới biết**. Bù lại lần đo đó xác nhận
  `BgeM3Embedder` chạy đúng với model thật, sạch với `transformers 5.5.4`.
- **Kiểm rẻ trước khi tốn GPU là đúng.** `--dry-run` + `--fake --limit 20 --smoke` bắt hết
  lỗi schema mà không cần GPU → session T4 chạy một phát ăn ngay.

## Trạng thái repo

**Dirty.** `M` `STATUS.md` · `M` `00-INDEX.md` · `??` handoff này.
5 commit trong phiên; 3 đã push (`57c2caa`), **2 chưa push**.
