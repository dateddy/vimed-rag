# Handoff — Session 10 (2026-09-06 → 07)

> Ảnh chụp trạng thái. Contract sống ở `brain/contracts/`, quyết định ở
> `brain/decisions/DECISIONS.md`, state đầy đủ ở `brain/state/STATUS.md`.
> File này **chỉ trỏ tới**, không lặp lại.

## Đã đóng trong phiên

**TUẦN 3 ĐÓNG.** T3.1 `HybridRetriever` · T3.2 `BgeReranker` · T3.4 đo retrieval ·
T3.6 Streamlit nối retriever thật · mở nhóm E 12 → 21 · chốt chi phí Tuần 7.

- Test set: **59 câu** = A18/B12/D8/**E21** (DEC-043). Mọi tiêu chí dựng + policy PASS.
- **123/123 test PASS.** Không test nào chạm mạng hay nạp model.
- Cấu hình triển khai: `top_k_dense=10`, `rerank_max_length=512` → **17,2s/truy vấn** CPU.
- **DEC-033 … DEC-043** đã ghi (11 dòng mới). DEC-037 supersedes DEC-033 phần backend;
  DEC-043 supersedes DEC-041 phần mục tiêu 35–40.
- 3 giới hạn mới đã vào `contracts/constraints.md`.

## Hai claim của đề tài bị dữ liệu bác bỏ — KHÔNG sửa được bằng code

1. **"Hybrid > Dense" không chứng minh được** (DEC-038). Mở test set 12 → 21 câu không
   cứu được: `dense/512` **0,714** đứng đầu, `hybrid/512` 0,667 — hướng **ngược** claim.
   Báo cáo phải trình bày hybrid như lựa chọn thiết kế, không phải kết quả thắng thua.
2. **Ngưỡng grader `0.6/0.3` không an toàn** (DEC-039). Ở 0.6 có **10/30 câu nhóm A/B
   vẫn được trả lời**. Dải an toàn đo được: **(+2,15, +2,71] logit**, điểm giữa +2,43 =
   sigmoid **0,919** → 17/21 câu E trả lời, **0/30 A/B lọt lưới**.
   **Chưa chốt vào `config.yaml`** vì chưa có tập giữ lại — điều kiện của DEC-039.

Phát hiện nền dưới cả hai: **score bám ĐỘ CÙNG CHỦ ĐỀ, không bám ĐỘ ĐÚNG.**
E-07 đạt +5,67 khi bài vàng **không hề vào pool**; E-12 có bài vàng hạng 1 mà chỉ
−0,89. Chi tiết + bảng ở STATUS mục "hạ tầng".

## Blocker

- **Không có blocker chặn build.**
- **Cluster Qdrant Cloud free tier NGỦ sau ~2 tuần không dùng.** Dấu hiệu: TCP 443 mở
  nhưng TLS reset. Vào console bấm resume, dữ liệu còn nguyên. Đừng debug `.env`/DNS.

## Việc kế tiếp

1. **`git push origin main`** — `origin/main` còn ở `57c2caa`, **21 commit đang treo**.
2. **TUẦN 4 — Generation (Gemini + citation).**
3. Cạm bẫy Tuần 4 đã ghi trong STATUS: cắt **byline** trước khi vào prompt (12,4% bài),
   và **chưa được nối grader vào đường thật** cho tới khi có ngưỡng hiệu chỉnh.

## Việc treo ngoài code

- **`data/processed/rerank_cache.json` (~1.900 logit ≈ 1,5 giờ CPU) CHỈ có trên máy này**
  — gitignore. Xoá `data/processed/` là mất, phải chấm lại từ đầu.
- **`KEEP_A`/`KEEP_B` khoá theo id ứng viên** nên không rebuild được từ clone sạch
  (nhóm E đã khoá theo chỉ mục ViMedAQA nên an toàn). ~20 phút để sửa. Cần trước bảo vệ.
- **ABSTAIN-do-retrieval vẫn mang theo chunk** (`pipeline.py:99`). UI Tuần 7 mà hiển thị
  thì "tôi không đủ căn cứ" lại kèm 5 nguồn trông thuyết phục.
- **Ngưỡng grader chưa có tập giữ lại** — Tuần 6 phải tách tập trước khi báo cáo số.
- **Biên `top_k_dense=10` chỉ còn 3 bậc** (hạng chunk xấu nhất là 7). Nếu nhóm E mở
  tiếp thì phải đo lại; câu nào rơi hạng >10 thì trả về 20 và chấp nhận 34,5s.
