# Handoff — Session 13 (2026-09-09 → 2026-09-10)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở `DECISIONS.md`,
> state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**TUẦN 6 VIỆC (1) ĐÓNG** — bảng Static vs Corrective **4 nhánh × 4 nhóm**
(`docs/static-vs-corrective.md`, thuần số, 0 lượt API). **DEC-059…062**,
`constraints.md` sửa 4 mục. **282/282 test PASS** (246 → 282). Nợ `stats.py`
**đã dọn** — 3 bản sao xoá, `docs/` byte-identical.

| # | nhánh | leakage A/B ↓ | coverage E ↑ | D bị chặn |
|---|---|---|---|---|
| 1 | LLM-only | — *(cần RAGAS)* | — | — |
| 2 | Static RAG | 100% † | 100% † | 0/8 † |
| 3 | **Corrective + guard — HỆ ĐANG CHẠY** | **0/30** | **17/21 = 81%** | 8/8 |
| 4 | Corrective không guard (tái lập) | 2/30 = 7% | 17/21 | 8/8 |

† = theo **cấu tạo**, không phải phép đo. Bảng tái lập đúng từng số DEC-051/056/057.

## Bốn kết quả đổi cách viết báo cáo

1. **✅ VÁ ĐƯỢC DEC-057** (DEC-061). Guard lượt 2: leakage **7% → 0/30**, coverage
   **không đổi**. 4 cách vá khác đã thử và **chết vì lý do đo được** — DEC-061 ghi
   đủ, đừng thử lại. ⚠️ Guard **vô hiệu hoá cái hại, không thành cái lợi**: cứu 0/4.
2. **⛔ `invalid_citations` MÙ với bịa nội dung** (DEC-059) — **0/30** trên lô có lỗi
   thật. Không thay được LLM judge → **RAGAS thành bắt buộc**, hết đường lùi.
3. **⛔ Lập luận policy gate của DEC-024 bị phép đo làm yếu** (DEC-060). Bỏ gate,
   Static RAG trả lời 8/8 nhưng **phần lớn tự thận trọng**; gate thắng **2/8**
   (`D-03` chẩn đoán · `D-05`). Biện hộ mới: đảm bảo thay vì xác suất.
4. **Soi tay 30 câu A/B** (DEC-062): leakage nội dung **6/30 = 20%**, **0/30 bịa
   tự do** — toàn bộ là **thay thế thực thể**. PA 3: giữ mẫu số, cờ, độ nhạy 14–16%.

## Blocker

- **Không có blocker chặn build.** Qdrant sống (dùng 2026-09-09).
  ⚠️ Theo dõi **credit OpenRouter** trước bảo vệ.

## Việc kế tiếp

1. **⚠️ `git push origin main` — treo 12 commit + ~22 file chưa commit.** Rủi ro
   lớn nhất: mất máy = mất 2 phiên và 8 kết quả. `origin/main` ở `0f00902`.
2. **Đạt DUYỆT LẠI NHÃN** — `static_leak_review.jsonl` + `concept_variants.jsonl`
   đang mang `labeled_by: claude-draft-pass`. Con số 20% **không được trích vào
   báo cáo** trước khi duyệt; provenance là một phần của kết quả (DEC-014).
3. **Tuần 6 việc (2) risk–coverage** + **(3) RAGAS** — cả hai còn stub
   `NotImplementedError`, và `ragas==0.4.3` vẫn comment trong `requirements.txt`.

## Việc treo ngoài code

- **PA 2** (vệ sinh, không hồi tố): `norm_keyword()` xử lý chữ số↔chữ cho lần dựng
  test set sau. **PA 4:** corpus có *trả lời được* `A-02/A-05/A-06` không — sơ bộ
  chỉ `A-05` đáng đổi nhãn.
- **`data/processed/` bị gitignore** → clone sạch KHÔNG có `runs.jsonl` (~45 phút) ·
  `static_rag.jsonl` (~2 phút, nhóm D cần `--retrieve-missing`) · `corpus.jsonl`.
  Nhãn tay ở `data/` nên **được commit** — đúng thiết kế.
- **Chi phí Tuần 7 phải chạy `export_runs.py --no-cache`** (số hiện tại có cache).
- **⚠️ Lỗi hiển thị chưa vá:** câu `ANSWER` hiện 5 nguồn cạnh câu nói "không có
  thông tin" (`A-01`, `E-21`) — vá ở tầng nào chưa chọn.
- **⚠️ `data/testset_e_patient.jsonl` giữ khuôn JSONL — mỗi bản ghi MỘT dòng.**
  **E2 evidence-highlighting coi như chết** (DEC-015).
