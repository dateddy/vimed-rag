# Handoff — Session 17 (2026-09-15)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở `DECISIONS.md`,
> state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**🎉 RỦI RO DEMO SỐ 1 ĐÓNG** · **DEC-071** · **DEC-072** · **399 test PASS**.

- **DEC-071 — việc treo rotate key Qdrant (mở từ Session 6) đóng.** Không rotate; tách
  key theo **QUYỀN**: Space dùng key **chỉ-đọc** giới hạn collection, `.env` local +
  Kaggle giữ key toàn quyền. Kèm `scripts/smoke_qdrant_readonly.py` (ĐỌC pass ∧ GHI fail).
- **DEC-072 — HF Space `datvu107-dateddy/vimed` RUNNING** (docker · cpu-basic ·
  private). Đo `"Triệu chứng tăng huyết áp?"`: **20,8 s** (ấm máy) · **15,8** · **15,9**.
  Không đổi `top_k_dense`, không dùng ZeroGPU. Dây chuyền deploy ở `deploy/hf-space/`
  (`Dockerfile` · `README.md` · `requirements.txt` · `DEPLOY.md` — đã chạy thật hết).

## ⚠️ Số dễ bị trích sai

**ĐỪNG so 15,9 s với 75–82 s.** Mốc kia đo ở `rerank_max_length=1024` + `top_k_dense=20`;
cấu hình **đang chạy** là `512` + `10` (`config.yaml:54-56`). Mốc đúng để so là **~17 s**
(dự đoán local cho chính cấu hình này, có trong STATUS **trước** khi đo). Space ra
15,8–20,8 s → **khớp dự đoán**: Spaces **ngang** máy local, thứ rẻ đi là **cấu hình**.

## Năm thứ đi ngược dự đoán — đọc trước khi tin plan cũ

1. **Câu hỏi của DEC-032 đặt sai trục.** Nó hỏi *rotate hay không*; cả hai nhánh đều
   để **key toàn quyền** trong Secrets của Space công khai — bán kính thiệt hại
   không đổi. Trục đúng là **quyền**. Tiền đề DEC-032 vẫn đứng: quét
   `git rev-list --all` tìm `eyJhbG` ra **0 hit**.
2. **HF bỏ SDK `streamlit`** → template **Docker**; repo tự mang `Dockerfile` **và tự
   cài `streamlit`**. Thiếu dòng đó: build xanh rồi chết `command not found`.
3. **HF bắt PRO $9/tháng** cho Space Docker trên `cpu-basic` (chỉ Static còn free).
   Streamlit Community Cloud loại bằng **số học**: 1 GB RAM vs ~4,5 GB model.
4. **`Set-Content -Encoding utf8` (PS 5.1) ghi BOM** — `EF BB BF` làm hỏng **đúng dòng
   ĐẦU** file secrets, HF **lặng lẽ** bỏ dòng đó. Set 2 biến, vào 1. Dùng
   `[System.IO.File]::WriteAllText`. → `QDRANT_URL` nay là **variable** (kiểm lại được).
5. **Space `private` → trình duyệt trả 404, không phải 403.** Vào qua
   `huggingface.co/spaces/...` khi đã đăng nhập.

## Việc kế tiếp

1. **Task 5 — UI đầu-cuối (C3 + B3 gộp).** Streamlit **vẫn là bản Tuần 3**: 2 tab,
   chưa tab nào chạy generator thật. **Bắt buộc** dùng
   `answer_content.is_refusal_text()` để ẩn khối nguồn ở câu từ chối — đó là chỗ vá
   đúng cho lỗi treo "5 nguồn cạnh câu nói không có thông tin" (`A-01`, `E-21`).
2. **Task 6 — latency p50/p95 tách stage + cost/1.000 query.**
   `export_runs.py --no-cache`; dùng số **đo** ~45 s (từ chối) / ~27 s (trả lời),
   **không** dùng 17,2 s của DEC-042.
3. **Deploy lại sau Task 5** rồi mới chuyển Space sang public (DoD Tuần 7 đòi link
   public). Lặp bước 4–5 của `DEPLOY.md`, hoặc `hf spaces hot-reload`.

## Việc treo ngoài code

- **⛔ Còn commit chưa push** (`71aa8fe` mang **nhãn người không tái tạo được**).
- **Space đang `private`** — chuyển public trước bảo vệ, kèm kiểm credit OpenRouter
  (`GET https://openrouter.ai/api/v1/key`). **PRO $9/tháng** — huỷ sau bảo vệ.
- Mục treo còn lại (`--pairable-only` help sai · `data/processed/` gitignore ·
  `ragas` 0.4.3 · khuôn JSONL `testset_e_patient` · đừng chép bản sao `stats.py` ·
  giọng bệnh nhân là cận dưới · rủi ro tiến độ 1/2 năng lực) **ở `STATUS.md`**.
