# Handoff — Session 14 (2026-09-11)

> Ảnh chụp trạng thái. Contract ở `brain/contracts/`, quyết định ở `DECISIONS.md`,
> state đầy đủ ở `STATUS.md`. File này **chỉ trỏ tới**.

## Đã đóng

**TUẦN 6 VIỆC (2) ĐÓNG — đường cong risk–coverage** (DEC-063).
`src/eval/risk_coverage.py` hết stub · `scripts/build_risk_coverage.py` →
`docs/risk-coverage.md` + `.png` · **316 test PASS** (282 → 316). Thuần số, 0 lượt API,
không cần Qdrant, tái lập **byte-identical**. **Điểm vận hành: coverage 17/21 ·
leakage 0/30 · risk 0,000 · `is_on_frontier = True`** (4/52 điểm sống sót phép loại trội).
**Cũng đã push** 17 commit treo của S11/12/13 → `origin/main` ở `58911bb`.

## Ba kết quả + một cái bẫy

1. **Vách 3,056 logit** — E thấp nhất được trả lời `+2,708`, E cao nhất bị từ chối
   `-0,348`, **giữa hai mốc không có câu nào**. Là "vực" DEC-051, nay tính từ dữ liệu.
2. **Giá của câu E kế tiếp = 9 câu A/B lọt lưới** — hạ ngưỡng 2,785 logit thì coverage
   17/21 → 19/21 nhưng leakage 0/30 → 9/30. Đường cong là **bậc thang**: câu hỏi có
   nghĩa là *"câu tiếp theo giá bao nhiêu"*, không phải *"đánh đổi mỗi điểm %"*.
3. **Dải miễn phí 2,412 logit** — coverage đứng yên 17/21 trong khi leakage tụt
   9/30 → 0/30. Đánh đổi liên tục thì dải này phải rỗng; nó không rỗng.
4. **⚠️ BẪY — `risk` phụ thuộc TỈ LỆ TEST SET.** Ở coverage tối đa `risk = 0,588` =
   đúng 30/51 = tỉ lệ câu A/B: thuộc tính của **test set**, không phải của hệ thống.
   Vì thế `dominates()` xếp trên `(cov_e, leak_ab)`, **không** trên `risk`.

⚠️ **Đường cong ĐỔI VAI:** không còn chọn ngưỡng (DEC-051 đã chốt bằng LOOCV) mà
**chứng minh** điểm vận hành nằm trên biên hiệu quả. Lý do: DEC-063.

## Audit đã chạy

1 lỗi MEDIUM **đã vá**: báo cáo in *"hạ ngưỡng 3,063"* — số đó đo từ `+2,7075`, không
từ ngưỡng đang chạy `+2,4288`; nay in `2,785` kèm **cả hai mốc**. Biên `>=` kiểm bằng
reproducer chạy thẳng vào `Grader` thật; đối chứng chéo **51/51 câu**, lệch `9,5e-07`.

## Blocker — không có cái nào chặn build

Qdrant sống (dùng 2026-09-09). ⚠️ Theo dõi **credit OpenRouter** trước bảo vệ.

## Việc kế tiếp

1. **`git push origin main`** — treo **3 commit** (`f9bb5cf` `72870aa` `87d9e80`).
2. **⚠️ CHỐT DÙNG CẬN TRÊN NÀO.** Repo đang **trộn hai phương pháp** cho cùng phép
   đo `0/30`: **Wilson** `0–11%` (STATUS + DEC-061) vs **quy tắc số ba** `< 10%`
   (`threshold-calibration.md`). Cả hai đúng; chọn một rồi quét sửa toàn repo.
3. **RAGAS — việc CUỐI của Tuần 6.** `run_ragas.py` vẫn stub, `ragas==0.4.3` vẫn
   comment. Quyết trước: cài hay không · judge Faithfulness = OpenAI frontier
   `temperature=0` (DEC-023) hay `FaithfulnesswithHHEM` (né bẫy judge cùng họ Gemini).
4. **Đạt DUYỆT LẠI NHÃN** (treo từ S13) — hai file nhãn vẫn `claude-draft-pass`;
   con số 20% chưa được trích vào báo cáo (DEC-014).

## Việc treo ngoài code

- **`data/processed/` gitignore** → clone sạch **không chạy được**
  `build_risk_coverage.py`: thiếu `calibration_scores.json` + `runs.jsonl` (~45 phút).
  Script dừng kèm hướng dẫn, không im lặng. `docs/risk-coverage.*` thì **đã commit**.
- `matplotlib==3.10.8` để **NGOÀI** khối GATED — lý do ghi trong `requirements.txt`.
- ⚠️ **Chưa vá:** `ANSWER` hiện 5 nguồn cạnh câu "không có thông tin" (`A-01`, `E-21`).
- Mục treo cũ (coverage đếm dư 1 · Tuần 7 `--no-cache` · khuôn JSONL) **ở `STATUS.md`**.
