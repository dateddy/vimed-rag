# DECISIONS — nhật ký quyết định (append-only)

> **Append-only. KHÔNG sửa dòng cũ.** Muốn đảo một quyết định → thêm dòng MỚI với
> `Supersedes: DEC-00X`, để dòng cũ nguyên vẹn làm lịch sử.
>
> Cột "Ai quyết": `A` = Đạt. (Trước 2026-08-08 `A` = Member A, `B` = Member B —
> từ DEC-013 dự án còn **1 người**, mọi dòng mới đều là `A`.)

> ✅ **DEC-001…DEC-006 và DEC-014 đã được GVHD duyệt ngày 2026-08-11** (xem DEC-019).
> Việc treo "xác nhận claim với GVHD" — mở từ Session 2 — đã đóng.

| ID | Ngày | Quyết định | Lý do (1 dòng) | Ai quyết | Supersedes |
|---|---|---|---|---|---|
| DEC-001 | 2026-07-04 | **Partial agentic**, giữ scope IN/OUT | Full agentic = tái hiện công trình 2023–24, phá tính lặp của eval | A | — |
| DEC-002 | 2026-07-04 | Framing = **calibrated abstention + risk–coverage** | Claim đo được thay vì claim khẳng định | A | — |
| DEC-003 | 2026-07-04 | **KHÔNG LangGraph** — Python thuần + `trace` list | 4 node + max_iter=1 → LangGraph là chi phí thuần (cold start, version churn) | A | — |
| DEC-004 | 2026-07-04 | Ablation chunk **256 vs 512**, dời về **Tuần 2** | Rẻ nhất, hay bị hỏi nhất; chạy lúc index gần free | A | — |
| DEC-005 | 2026-07-04 | Safety subset **50 câu nhãn THEO CẤU TRÚC (A–E)** | Không có bác sĩ; LLM-as-judge = vòng luẩn quẩn (judge cùng họ generator) | A | — |
| DEC-006 | 2026-07-04 | E2 evidence-highlighting = **buffer cắt được** | Đẹp portfolio nhưng không đóng góp claim | A | — |
| DEC-007 | 2026-08-07 | **Gate 0 = GO.** Chốt ngưỡng eval = **40 QA** và định nghĩa **NO-GO #2**; đóng 3 `TODO(unknown)` ở `gates.md` | Pass cả 3 tiêu chí, không cái nào ở biên (coverage dư ~100×, alignment 30/30). Ngưỡng 40 lấy từ `MIN_EVAL_QA_TOTAL` trong chính script mà contract trỏ tới, không phải số bịa | A | — |
| DEC-008 | 2026-08-07 | Corpus = split **`vinmec_article_content`** (32,604 bài), KHÔNG phải `train` | Split `train` **không tồn tại** trong `urnus11/Vietnamese-Healthcare`. Chọn `article_content` vì đúng nghĩa "bài" của tiêu chí #1 + đúng deliverable Tuần 1. Bỏ `vinmec_article_subtitle` (163k mục con — sát chunk hơn nhưng đếm sai đơn vị "bài") | A | — |
| DEC-009 | 2026-08-07 | Eval = **`tmnam20/ViMedAQA`** split `train`, config `all` | Giải placeholder `"ViMedAQA"` thành path HF thật đã verify (public, không gated). Nguồn youmed.vn — **khác nguồn corpus** (vinmec.com), xem giới hạn #1 trong `gates.md` | A | — |
| DEC-010 | 2026-08-07 | Siết `config/specialties.yaml`: bỏ từ đơn (`tim`, `động mạch`, `tai biến`, `nhồi máu`, `loạn nhịp`, `xơ vữa`, `đái tháo`), thay bằng cụm đặc hiệu; bỏ 6 keyword thừa (`solo=0`) | Audit trên 32,543 bài thật: `tai biến` bắt "tai biến gây mê/mổ lấy thai" (sản, gây mê), `động mạch` bắt "động mạch lách/phế quản", `đái tháo` bắt **`đái tháo nhạt`** (bệnh tuyến yên, khác hẳn), `tim` bắt **`tim thai`** (sản). Gate 0 alignment vẫn PASS sau khi siết | A | — |
| DEC-011 | 2026-08-07 | **Đột quỵ KHÔNG thuộc `tim_mach`** | Phân khoa VN xếp đột quỵ vào thần kinh; giữ scope 2 khoa sạch để hội đồng không hỏi "sao bài thần kinh nằm trong corpus tim mạch". Muốn theo CVD của WHO thì thêm lại 2 dòng đã ghi sẵn trong file | A | — |
| DEC-012 | 2026-08-07 | **Chạy lại Gate 0 sau mỗi lần đổi `specialties.yaml`**; verdict vẫn GO sau DEC-010/011 | Siết keyword làm eval tụt 3,423 → 2,645 QA và corpus 16,869 → 14,618 bài. Verdict cũ đo trên từ vựng cũ nên hết hiệu lực nếu không chạy lại. 3 script audit (`policy_audit` · `kw_audit` · `alignment_audit`) đưa vào `scripts/` để tái lập được | A | — |
| DEC-013 | 2026-08-08 | **Dự án còn 1 người (Đạt).** Toàn bộ workstream của Member B (generation · test set · eval/RAGAS · UI · báo cáo) chuyển về Đạt. Xoá `state/STATUS-B.md`, đổi `state/STATUS-A.md` → `state/STATUS.md`; bỏ vai "Member A/B" khỏi `CLAUDE.md`, `00-INDEX.md`, plan | Member B rời dự án. Giữ 2 file state cho 1 người là nợ context: mỗi session phải đọc 1 file trống rồi đoán "trống = chưa làm" hay "trống = không còn ai". Scope kỹ thuật **không đổi** — chỉ đổi ai làm và lịch | A | — |
| DEC-014 | 2026-08-08 | **Bỏ Cohen's κ. Bỏ nhóm C** khỏi safety subset; giữ 4 nhóm **A18 / B12 / D8 / E12 = 50 câu** | κ là *inter-annotator* agreement — cần 2 người gán độc lập, 1 người thì đại lượng này không tồn tại (test–retest tự gán chỉ đo trí nhớ của chính mình). Nhóm C ("thiếu thông tin") là nhóm DUY NHẤT mà nhãn đến từ phán đoán người, không kiểm chứng được bằng máy hay policy → bỏ C là mất ít nhất mà cứu được nguyên tắc "mọi nhãn đều kiểm chứng được": A/B bằng script, D bằng policy tự công bố, E bằng ViMedAQA ground truth. Giữ tổng 50 bằng cách dồn sang A/B/E (rẻ, sinh bằng script). **Limitations phải ghi: không có inter-annotator agreement** | A | DEC-005 (phần κ + nhóm C) |
| DEC-015 | 2026-08-08 | Giữ **nguyên scope**; thứ tự cắt khi kẹt vẫn là E2 evidence-highlighting → ablation reranker on/off → ablation chunk 256 vs 512. **Chưa cắt gì lúc này** | Năng lực giảm 1/2 nhưng thứ tự ưu tiên đã đúng từ trước (DEC-006), không cần quyết định mới. Cắt sớm khi chưa biết trượt bao nhiêu = vứt kết quả không cần thiết. Chốt điểm kiểm tra: **hết Tuần 5 mà safety subset chưa có nhãn → cắt E2 ngay**, không chờ tới Tuần 7 | A | — |
| DEC-016 | 2026-08-08 | **Chính sách gán khoa = "keyword ở TITLE HOẶC ≥25 lần trong bài"** (thay "có mặt ở đâu cũng tính"). Corpus chốt: **tim_mach 727 · tieu_duong 698 = 1.425 bài**, both 1.1% | Hai lý do độc lập, cái nào cũng đủ loại chính sách cũ. (1) **`both%` đo sai thứ**: nó bão hoà ở 2% từ ngưỡng ≥15 trong khi 80% bài `tim_mach` vẫn lọt vào chỉ nhờ đếm keyword trong thân bài. Đo `title-hit` (tỉ lệ bài được gán có keyword ở tiêu đề) mới thấy thật: ngưỡng ≥8 chỉ đạt 19% — soi tay thấy "Nho khô có tốt cho bạn không?", "Ăn hạt mít có tác dụng gì?" bị gán tim mạch; "khoa trội" còn tệ hơn (9%). ≥25 đạt 61% và cân 2 khoa gần 1:1. (2) **Chính sách cũ không deploy được**: 171.527 vector = **89% Qdrant Cloud free 1GB** trước khi cộng sparse vector + index overhead, mà Tuần 2 phải index HAI collection (DEC-004). ≥25 chỉ dùng 8%. Gate 0 tái kiểm dưới chính sách mới: coverage PASS (727/698 ≥ 150), eval 2.645 QA không đổi, alignment **PASS 29/30 (97%)** median 0.378. Tái lập: `scripts/policy_cost_audit.py` + `scripts/alignment_audit.py` | A | — |
| DEC-017 | 2026-08-09 | **Data contract của loader.** (a) `RawDocument.specialties` (tuple, chứa **TẤT CẢ** khoa khớp) là trường dùng để **lọc khoa**; `specialty` giữ nguyên nghĩa cũ = khoa đầu theo thứ tự khai báo, chỉ để hiển thị/payload 1 giá trị. (b) Ngưỡng chính sách gán khoa đặt ở `config/config.yaml` khối `data`, **KHÔNG** ở `config/specialties.yaml` | (a) 15 bài (1.1%) thuộc cả 2 khoa. Đổi `specialty` thành list sẽ phá interface `RetrievedChunk`/payload đã có; giữ mỗi trường đơn thì Tuần 3 lọc payload Qdrant sẽ **âm thầm đánh rơi** 15 bài khỏi một khoa — lỗi không có test nào bắt được vì kết quả vẫn "có bài trả về". Thêm trường plural là cách rẻ nhất giữ cả hai. (b) `specialties.yaml` là map phẳng `khoa -> keyword` và `scripts/gate0_data_check.load_specialty_keywords()` đọc **mọi key cấp 1 làm tên khoa** → thêm key chính sách vào đó sẽ sinh ra một "khoa" giả và làm hỏng cả 3 script audit đang dùng để tái lập số của Gate 0 | A | — |
| DEC-018 | 2026-08-11 | **Vector store = Qdrant Cloud (free tier 1GB), KHÔNG dựng Docker local.** `docker-compose.yml` hạ xuống thành phương án dự phòng offline; thêm `QdrantConfig.api_key` (mặc định `""`) đọc từ `${QDRANT_API_KEY}` | Docker Desktop **chưa hề được cài trên máy** (`docker` không có trên PATH) — "Qdrant chạy" của DoD Tuần 1 thực ra là 1–2 tiếng cài WSL2 + Docker, không phải một lệnh `docker-compose up -d` như handoff Session 5 tưởng. Mà Tuần 7 deploy HF Spaces thì **bắt buộc** phải có Qdrant có thể truy cập từ internet → local chỉ là chặng trung gian sẽ phải migrate. Ngân sách đã tính sẵn ở DEC-016: cả 2 collection (chunk 256 + 512) chỉ dùng **~8% của 1GB free** → không có rủi ro tràn. Đánh đổi chấp nhận: phụ thuộc mạng khi debug retrieval Tuần 3, và dữ liệu y tế công khai (vinmec.com) nên đưa lên cloud không phát sinh vấn đề riêng tư | A | — |
| DEC-020 | 2026-08-13 | **Giữ nguyên chunking `size=512, overlap=50`. KHÔNG làm snap-to-sentence, KHÔNG sửa `cleaner.py`.** Chốt sau khi soi 25 chunk bằng mắt trên corpus thật — đây là lần cuối sửa còn rẻ, và kết luận là không cần sửa | Cờ tự động trên **toàn bộ** 4.972 chunk (`scripts/chunk_audit.py`): sót HTML/URL **0,2%** · chunk có >25% nội dung là boilerplate Vinmec **0** · bảng số bị ép phẳng **0,1%** · byline "Bài viết được tư vấn chuyên môn bởi…" chỉ **0,16%** token toàn corpus (17,6% bài × ~19 token) → cả 3 tiêu chí "rác HTML / menu-footer / bảng vỡ" đều PASS bằng máy, không cần sửa gì. Còn lại đúng một câu hỏi máy không trả lời được: **73% chunk kết thúc giữa câu (512), 85% (256)** — soi tay 25 mẫu thì **không mẫu nào mất nghĩa**, chỉ "hơi thừa" (mẫu 1) và "hơi cụt nhưng vẫn hiểu" (mẫu 9, 10). Lý do: `overlap=50` đã gánh — câu bị cắt ở đầu chunk N luôn nằm **trọn vẹn** ở cuối chunk N−1 (thấy rõ ở mẫu 2, 6, 7), nên cắt giữa câu ở đây là vấn đề **thẩm mỹ, không phải mất thông tin**. Snap-to-sentence sẽ làm độ dài chunk dao động và thêm một tham số phải biện minh trước hội đồng, đổi lấy một lợi ích chưa đo được → không đáng. **Điều kiện đảo quyết định:** nếu Tuần 3 đo retrieval thấy nhiễu do ranh giới chunk thì nâng `overlap` 50 → 80 **trước** (rẻ, không đụng code, storage +6%), snap-to-sentence chỉ là phương án cuối | A | — |
| DEC-019 | 2026-08-11 | **GVHD duyệt DEC-001…DEC-006 và DEC-014.** Framing "calibrated abstention + risk–coverage", partial agentic, không LangGraph, E2 là buffer cắt được, **và việc bỏ Cohen's κ + bỏ nhóm C** đều được chấp nhận. Đã báo dự án còn 1 người | Đóng việc treo lớn nhất của dự án (mở từ Session 2). Quan trọng nhất: **DEC-014 không còn phải đảo trước Tuần 5** — rủi ro "thầy bắt làm inter-annotator agreement mà chỉ có 1 người" đã tắt hẳn, safety subset chốt ở 4 nhóm A18/B12/D8/E12. Limitations trong báo cáo vẫn **phải** ghi "không có inter-annotator agreement" — được duyệt không có nghĩa là được giấu | GVHD | — |

<!-- TODO(unknown): ngày chính xác các quyết định — COMPRESS ghi "session này" (Session 2);
     dùng 2026-07-04 khớp tên file handoff archive. Xác nhận nếu cần ngày thật. -->
