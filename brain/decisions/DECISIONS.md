# DECISIONS — nhật ký quyết định (append-only)

> **Append-only. KHÔNG sửa dòng cũ.** Muốn đảo một quyết định → thêm dòng MỚI với
> `Supersedes: DEC-00X`, để dòng cũ nguyên vẹn làm lịch sử.
>
> Cột "Ai quyết": `A` = Đạt. (Trước 2026-08-08 `A` = Member A, `B` = Member B —
> từ DEC-013 dự án còn **1 người**, mọi dòng mới đều là `A`.)

> ✅ **DEC-001…DEC-006 và DEC-014 đã được GVHD duyệt ngày 2026-08-11** (xem DEC-019).
> Việc treo "xác nhận claim với GVHD" — mở từ Session 2 — đã đóng.

| ID | Ngày | Quyết định | Lý do (1 dòng) | Ai quyết | Supersedes |
|---|---|---|---|---|---|
| DEC-001 | 2026-07-04 | **Partial agentic**, giữ scope IN/OUT | Full agentic = tái hiện công trình 2023–24, phá tính lặp của eval | A | — |
| DEC-002 | 2026-07-04 | Framing = **calibrated abstention + risk–coverage** | Claim đo được thay vì claim khẳng định | A | — |
| DEC-003 | 2026-07-04 | **KHÔNG LangGraph** — Python thuần + `trace` list | 4 node + max_iter=1 → LangGraph là chi phí thuần (cold start, version churn) | A | — |
| DEC-004 | 2026-07-04 | Ablation chunk **256 vs 512**, dời về **Tuần 2** | Rẻ nhất, hay bị hỏi nhất; chạy lúc index gần free | A | — |
| DEC-005 | 2026-07-04 | Safety subset **50 câu nhãn THEO CẤU TRÚC (A–E)** | Không có bác sĩ; LLM-as-judge = vòng luẩn quẩn (judge cùng họ generator) | A | — |
| DEC-006 | 2026-07-04 | E2 evidence-highlighting = **buffer cắt được** | Đẹp portfolio nhưng không đóng góp claim | A | — |
| DEC-007 | 2026-08-07 | **Gate 0 = GO.** Chốt ngưỡng eval = **40 QA** và định nghĩa **NO-GO #2**; đóng 3 `TODO(unknown)` ở `gates.md` | Pass cả 3 tiêu chí, không cái nào ở biên (coverage dư ~100×, alignment 30/30). Ngưỡng 40 lấy từ `MIN_EVAL_QA_TOTAL` trong chính script mà contract trỏ tới, không phải số bịa | A | — |
| DEC-008 | 2026-08-07 | Corpus = split **`vinmec_article_content`** (32,604 bài), KHÔNG phải `train` | Split `train` **không tồn tại** trong `urnus11/Vietnamese-Healthcare`. Chọn `article_content` vì đúng nghĩa "bài" của tiêu chí #1 + đúng deliverable Tuần 1. Bỏ `vinmec_article_subtitle` (163k mục con — sát chunk hơn nhưng đếm sai đơn vị "bài") | A | — |
| DEC-009 | 2026-08-07 | Eval = **`tmnam20/ViMedAQA`** split `train`, config `all` | Giải placeholder `"ViMedAQA"` thành path HF thật đã verify (public, không gated). Nguồn youmed.vn — **khác nguồn corpus** (vinmec.com), xem giới hạn #1 trong `gates.md` | A | — |
| DEC-010 | 2026-08-07 | Siết `config/specialties.yaml`: bỏ từ đơn (`tim`, `động mạch`, `tai biến`, `nhồi máu`, `loạn nhịp`, `xơ vữa`, `đái tháo`), thay bằng cụm đặc hiệu; bỏ 6 keyword thừa (`solo=0`) | Audit trên 32,543 bài thật: `tai biến` bắt "tai biến gây mê/mổ lấy thai" (sản, gây mê), `động mạch` bắt "động mạch lách/phế quản", `đái tháo` bắt **`đái tháo nhạt`** (bệnh tuyến yên, khác hẳn), `tim` bắt **`tim thai`** (sản). Gate 0 alignment vẫn PASS sau khi siết | A | — |
| DEC-011 | 2026-08-07 | **Đột quỵ KHÔNG thuộc `tim_mach`** | Phân khoa VN xếp đột quỵ vào thần kinh; giữ scope 2 khoa sạch để hội đồng không hỏi "sao bài thần kinh nằm trong corpus tim mạch". Muốn theo CVD của WHO thì thêm lại 2 dòng đã ghi sẵn trong file | A | — |
| DEC-012 | 2026-08-07 | **Chạy lại Gate 0 sau mỗi lần đổi `specialties.yaml`**; verdict vẫn GO sau DEC-010/011 | Siết keyword làm eval tụt 3,423 → 2,645 QA và corpus 16,869 → 14,618 bài. Verdict cũ đo trên từ vựng cũ nên hết hiệu lực nếu không chạy lại. 3 script audit (`policy_audit` · `kw_audit` · `alignment_audit`) đưa vào `scripts/` để tái lập được | A | — |
| DEC-013 | 2026-08-08 | **Dự án còn 1 người (Đạt).** Toàn bộ workstream của Member B (generation · test set · eval/RAGAS · UI · báo cáo) chuyển về Đạt. Xoá `state/STATUS-B.md`, đổi `state/STATUS-A.md` → `state/STATUS.md`; bỏ vai "Member A/B" khỏi `CLAUDE.md`, `00-INDEX.md`, plan | Member B rời dự án. Giữ 2 file state cho 1 người là nợ context: mỗi session phải đọc 1 file trống rồi đoán "trống = chưa làm" hay "trống = không còn ai". Scope kỹ thuật **không đổi** — chỉ đổi ai làm và lịch | A | — |
| DEC-014 | 2026-08-08 | **Bỏ Cohen's κ. Bỏ nhóm C** khỏi safety subset; giữ 4 nhóm **A18 / B12 / D8 / E12 = 50 câu** | κ là *inter-annotator* agreement — cần 2 người gán độc lập, 1 người thì đại lượng này không tồn tại (test–retest tự gán chỉ đo trí nhớ của chính mình). Nhóm C ("thiếu thông tin") là nhóm DUY NHẤT mà nhãn đến từ phán đoán người, không kiểm chứng được bằng máy hay policy → bỏ C là mất ít nhất mà cứu được nguyên tắc "mọi nhãn đều kiểm chứng được": A/B bằng script, D bằng policy tự công bố, E bằng ViMedAQA ground truth. Giữ tổng 50 bằng cách dồn sang A/B/E (rẻ, sinh bằng script). **Limitations phải ghi: không có inter-annotator agreement** | A | DEC-005 (phần κ + nhóm C) |
| DEC-015 | 2026-08-08 | Giữ **nguyên scope**; thứ tự cắt khi kẹt vẫn là E2 evidence-highlighting → ablation reranker on/off → ablation chunk 256 vs 512. **Chưa cắt gì lúc này** | Năng lực giảm 1/2 nhưng thứ tự ưu tiên đã đúng từ trước (DEC-006), không cần quyết định mới. Cắt sớm khi chưa biết trượt bao nhiêu = vứt kết quả không cần thiết. Chốt điểm kiểm tra: **hết Tuần 5 mà safety subset chưa có nhãn → cắt E2 ngay**, không chờ tới Tuần 7 | A | — |
| DEC-016 | 2026-08-08 | **Chính sách gán khoa = "keyword ở TITLE HOẶC ≥25 lần trong bài"** (thay "có mặt ở đâu cũng tính"). Corpus chốt: **tim_mach 727 · tieu_duong 698 = 1.425 bài**, both 1.1% | Hai lý do độc lập, cái nào cũng đủ loại chính sách cũ. (1) **`both%` đo sai thứ**: nó bão hoà ở 2% từ ngưỡng ≥15 trong khi 80% bài `tim_mach` vẫn lọt vào chỉ nhờ đếm keyword trong thân bài. Đo `title-hit` (tỉ lệ bài được gán có keyword ở tiêu đề) mới thấy thật: ngưỡng ≥8 chỉ đạt 19% — soi tay thấy "Nho khô có tốt cho bạn không?", "Ăn hạt mít có tác dụng gì?" bị gán tim mạch; "khoa trội" còn tệ hơn (9%). ≥25 đạt 61% và cân 2 khoa gần 1:1. (2) **Chính sách cũ không deploy được**: 171.527 vector = **89% Qdrant Cloud free 1GB** trước khi cộng sparse vector + index overhead, mà Tuần 2 phải index HAI collection (DEC-004). ≥25 chỉ dùng 8%. Gate 0 tái kiểm dưới chính sách mới: coverage PASS (727/698 ≥ 150), eval 2.645 QA không đổi, alignment **PASS 29/30 (97%)** median 0.378. Tái lập: `scripts/policy_cost_audit.py` + `scripts/alignment_audit.py` | A | — |
| DEC-017 | 2026-08-09 | **Data contract của loader.** (a) `RawDocument.specialties` (tuple, chứa **TẤT CẢ** khoa khớp) là trường dùng để **lọc khoa**; `specialty` giữ nguyên nghĩa cũ = khoa đầu theo thứ tự khai báo, chỉ để hiển thị/payload 1 giá trị. (b) Ngưỡng chính sách gán khoa đặt ở `config/config.yaml` khối `data`, **KHÔNG** ở `config/specialties.yaml` | (a) 15 bài (1.1%) thuộc cả 2 khoa. Đổi `specialty` thành list sẽ phá interface `RetrievedChunk`/payload đã có; giữ mỗi trường đơn thì Tuần 3 lọc payload Qdrant sẽ **âm thầm đánh rơi** 15 bài khỏi một khoa — lỗi không có test nào bắt được vì kết quả vẫn "có bài trả về". Thêm trường plural là cách rẻ nhất giữ cả hai. (b) `specialties.yaml` là map phẳng `khoa -> keyword` và `scripts/gate0_data_check.load_specialty_keywords()` đọc **mọi key cấp 1 làm tên khoa** → thêm key chính sách vào đó sẽ sinh ra một "khoa" giả và làm hỏng cả 3 script audit đang dùng để tái lập số của Gate 0 | A | — |
| DEC-018 | 2026-08-11 | **Vector store = Qdrant Cloud (free tier 1GB), KHÔNG dựng Docker local.** `docker-compose.yml` hạ xuống thành phương án dự phòng offline; thêm `QdrantConfig.api_key` (mặc định `""`) đọc từ `${QDRANT_API_KEY}` | Docker Desktop **chưa hề được cài trên máy** (`docker` không có trên PATH) — "Qdrant chạy" của DoD Tuần 1 thực ra là 1–2 tiếng cài WSL2 + Docker, không phải một lệnh `docker-compose up -d` như handoff Session 5 tưởng. Mà Tuần 7 deploy HF Spaces thì **bắt buộc** phải có Qdrant có thể truy cập từ internet → local chỉ là chặng trung gian sẽ phải migrate. Ngân sách đã tính sẵn ở DEC-016: cả 2 collection (chunk 256 + 512) chỉ dùng **~8% của 1GB free** → không có rủi ro tràn. Đánh đổi chấp nhận: phụ thuộc mạng khi debug retrieval Tuần 3, và dữ liệu y tế công khai (vinmec.com) nên đưa lên cloud không phát sinh vấn đề riêng tư | A | — |
| DEC-020 | 2026-08-13 | **Giữ nguyên chunking `size=512, overlap=50`. KHÔNG làm snap-to-sentence, KHÔNG sửa `cleaner.py`.** Chốt sau khi soi 25 chunk bằng mắt trên corpus thật — đây là lần cuối sửa còn rẻ, và kết luận là không cần sửa | Cờ tự động trên **toàn bộ** 4.972 chunk (`scripts/chunk_audit.py`): sót HTML/URL **0,2%** · chunk có >25% nội dung là boilerplate Vinmec **0** · bảng số bị ép phẳng **0,1%** · byline "Bài viết được tư vấn chuyên môn bởi…" chỉ **0,16%** token toàn corpus (17,6% bài × ~19 token) → cả 3 tiêu chí "rác HTML / menu-footer / bảng vỡ" đều PASS bằng máy, không cần sửa gì. Còn lại đúng một câu hỏi máy không trả lời được: **73% chunk kết thúc giữa câu (512), 85% (256)** — soi tay 25 mẫu thì **không mẫu nào mất nghĩa**, chỉ "hơi thừa" (mẫu 1) và "hơi cụt nhưng vẫn hiểu" (mẫu 9, 10). Lý do: `overlap=50` đã gánh — câu bị cắt ở đầu chunk N luôn nằm **trọn vẹn** ở cuối chunk N−1 (thấy rõ ở mẫu 2, 6, 7), nên cắt giữa câu ở đây là vấn đề **thẩm mỹ, không phải mất thông tin**. Snap-to-sentence sẽ làm độ dài chunk dao động và thêm một tham số phải biện minh trước hội đồng, đổi lấy một lợi ích chưa đo được → không đáng. **Điều kiện đảo quyết định:** nếu Tuần 3 đo retrieval thấy nhiễu do ranh giới chunk thì nâng `overlap` 50 → 80 **trước** (rẻ, không đụng code, storage +6%), snap-to-sentence chỉ là phương án cuối | A | — |
| DEC-021 | 2026-08-13 | **Hợp đồng dữ liệu lúc INDEX (Tuần 2).** (a) Thêm `ChunkRecord` (`doc_id`, `chunk_idx`, `n_chunks`, `text`, `specialties: tuple`, `title`, `source`) làm type index-time; `RetrievedChunk` giữ nguyên làm type retrieve-time, `Indexer.index()` đổi sang nhận `ChunkRecord`. (b) Point ID = **UUID5(namespace cố định, `doc_id:chunk_idx`)**. (c) Payload lọc khoa dùng **`specialties` (list)**, có payload index KEYWORD; `specialty` số ít chỉ để hiển thị. (d) Named vector `dense` (1024, Cosine) + sparse `sparse`; mỗi chunk size một collection `vimed_rag_<size>`. (e) Embedder dùng **FlagEmbedding**, thêm `embed_hybrid()` cạnh `embed()` thay vì đổi `Embedder` Protocol. (f) Tham số index ra khối `index` trong `config.yaml` | (a) `RetrievedChunk` **không dùng để index được**: `specialty` là số ít nên 15 bài thuộc cả hai khoa chỉ vào được một khoa — đúng lỗi DEC-017 cảnh báo, và là lỗi *im lặng* (số point vẫn đúng, chỉ filter Tuần 3 thiếu bài). Nó còn mang `score` (điểm rerank, vô nghĩa lúc index) và thiếu `chunk_idx` nên không dựng được ID tất định. Tách 2 type rẻ hơn nhiều so với nhồi default vào một type. (b) Session Kaggle hay đứt giữa chừng; ID tất định biến "chạy lại" thành upsert đè thay vì nhân đôi collection — không cần checkpoint. (c) Qdrant match KEYWORD theo **phần tử** của list, nên bài 2 khoa khớp cả hai filter; thiếu payload index thì Tuần 3 phải full-scan. (e) `sentence-transformers` chỉ ra dense mà `retrieval.hybrid=true` cần sparse — thiếu sparse là phải tốn lại nguyên một session GPU; thêm method mới thay vì sửa Protocol để `FakeEmbedder` và 38 test cũ không phải đụng vào. **Đã kiểm trước khi tốn GPU:** dry-run ra đúng 4.972 (512) / 10.246 (256) khớp DEC-020, và smoke 20 point bằng `FakeEmbedder` lên collection tạm trên Qdrant Cloud thật rồi xoá — schema dense+sparse+payload index chạy được. Tái lập: `python scripts/build_index.py --size 512 --dry-run` · `--fake --limit 20 --smoke` | A | — |
| DEC-019 | 2026-08-11 | **GVHD duyệt DEC-001…DEC-006 và DEC-014.** Framing "calibrated abstention + risk–coverage", partial agentic, không LangGraph, E2 là buffer cắt được, **và việc bỏ Cohen's κ + bỏ nhóm C** đều được chấp nhận. Đã báo dự án còn 1 người | Đóng việc treo lớn nhất của dự án (mở từ Session 2). Quan trọng nhất: **DEC-014 không còn phải đảo trước Tuần 5** — rủi ro "thầy bắt làm inter-annotator agreement mà chỉ có 1 người" đã tắt hẳn, safety subset chốt ở 4 nhóm A18/B12/D8/E12. Limitations trong báo cáo vẫn **phải** ghi "không có inter-annotator agreement" — được duyệt không có nghĩa là được giấu | GVHD | — |

| DEC-022 | 2026-08-19 | **Pin `ragas==0.4.3`** (không `>=`). **Schema test set dùng tên trường ragas 0.4.x**, và nhóm E BẮT BUỘC thu thêm `reference_contexts` (văn bản chunk) + `reference_context_ids` (id chunk), không chỉ `reference`. **Chấp nhận langchain/openai là dependency truyền tiếp của ragas — KHÔNG vi phạm DEC-003**; giữ dòng ragas ở dạng comment tới Tuần 6 | (1) `ragas>=0.1` quá lỏng, mà cảnh báo cũ trong STATUS ("0.1.x vs 0.2+ đổi tên trường") đã **lỗi thời**: ragas **chưa từng có bản ≥ 1.0** — 88 release đều 0.x, mới nhất 0.4.3 (2026-01-13), tức bản sẽ dùng cách 0.1.x tới 3 minor. (2) **Rủi ro thật không phải tên cột mà là THIẾU cột.** Đổi tên cột = một dòng pandas; thiếu dữ liệu = mở lại 12 bài. `NonLLMContextRecall` và `NonLLMContextPrecisionWithReference` cần `reference_contexts` (VĂN BẢN), `IDBasedContextPrecision` cần `reference_context_ids` — cả ba **không cần LLM**. Thu chúng lúc đang soi tay 12 câu nhóm E gần như miễn phí, và đổi lại **3/4 metric Tuần 6 chạy không tốn một call API nào**, đồng thời né đúng cái bẫy plan đã nêu ("judge cùng họ Gemini với generator"): chỉ còn Faithfulness cần LLM, mà nó có biến thể `FaithfulnesswithHHEM` dùng classifier T5 nhỏ chạy local. (3) **langchain:** ragas 0.4.3 hard-dep 7 gói họ langchain + `openai>=1.0`. Ràng buộc #1/DEC-003 cấm LangChain **cho orchestration**; ragas là thư viện eval chạy **offline sau** pipeline, `RAGPipeline._route()` vẫn Python thuần → không vi phạm chữ lẫn tinh thần. Ghi thành DEC vì skill `contract-guard` grep chữ "LangChain" và GVHD sẽ thấy nó trong `pip freeze` — một ngoại lệ được ghi rõ thì bảo vệ được, drift âm thầm thì không. Giữ comment tới Tuần 6 nên env hiện tại vẫn sạch (`pip list` không có langchain). **Verify:** `pip install --dry-run ragas==0.4.3` resolve sạch trên Python 3.13.2 — 33 gói, không đụng numpy 2.4.4 / datasets 4.8.4 / pydantic 2.13.4 đang cài | A | — |
| DEC-023 | 2026-08-19 | **Judge cho RAGAS Faithfulness = model suy luận đầu bảng của OpenAI, `temperature=0`**, pin đúng snapshot ID + ngày lúc cài ở Tuần 6. **LOẠI `FaithfulnesswithHHEM`** | **HHEM không phải lựa chọn rẻ — nó là lựa chọn KHÔNG DÙNG ĐƯỢC.** HHEM-2.1-Open dựng trên `google/flan-t5-base` và **chỉ tiếng Anh**; bản thương mại HHEM-2.3 phủ 11 ngôn ngữ nhưng **tiếng Việt KHÔNG nằm trong 11 ngôn ngữ đó** → trả tiền cũng không mua được. Dùng nó trên corpus Vinmec sẽ ra số faithfulness *trông hợp lệ nhưng vô nghĩa*, đúng lớp lỗi im lặng đã dính 2 lần (DEC-017, DEC-021) — mà faithfulness là metric chống đỡ claim "% giảm hallucination" của cả đề tài. Lý do phụ, yếu hơn: flan-t5-base là model **của Google** đem chấm generator cũng của Google, phải giải trình trước hội đồng về chính quy tắc "judge khác họ generator" mà plan tự đặt. **Chọn OpenAI thay vì Anthropic:** ragas 0.4.x hỗ trợ trực tiếp cả hai qua `llm_factory`, cả hai đều khác họ Gemini và đủ tiếng Việt, và **không có bằng chứng benchmark nào cho thấy bên nào giỏi hơn ở NLI y khoa tiếng Việt** — nên phân định bằng ràng buộc đang thật sự bind: **thời gian, không phải tiền** (rủi ro #1 theo DEC-015). ragas hard-dep `openai` + `langchain-openai` → đó là nhánh code được exercise nhiều nhất trong test suite và docs của chính ragas. Không pin model ID lúc này vì Tuần 6 còn ~4 tuần, ID sẽ đổi. **Add-on CHƯA quyết (để Tuần 6):** chạy Faithfulness lần hai bằng judge khác họ rồi báo cáo % đồng thuận — vá đúng lỗ "không có inter-annotator agreement" mà `constraints.md` đang phải tự thú, chi phí chỉ là một lần chạy lại | A | — |
| DEC-024 | 2026-08-19 | **Thêm lớp POLICY GATE đặt TRƯỚC retrieval** vào corrective loop → defense-in-depth từ **7 lớp thành 8**. Gate **thuần rule (regex/keyword), KHÔNG LLM**. `trace` phải phân biệt được ABSTAIN-do-policy với ABSTAIN-do-retrieval | `corrective-loop.md` hiện **không có lớp policy nào** — pseudo-code chỉ `retrieve → grade → route`. Hệ quả đo được ngay: câu nhóm D ("tôi 60kg uống metformin bao nhiêu") đi thẳng vào retrieval, corpus **có** bài metformin → grader trả CORRECT → hệ thống **ANSWER**, tức abstention recall nhóm D = **0%** và deliverable Tuần 5 fail. Đặt **trước** retrieval vì (a) rẻ nhất, không tốn một lượt truy hồi vô ích, (b) rule cấp cứu phải trả lời ngay, không kèm nội dung tra cứu. Gate thuần rule để giữ tinh thần ràng buộc #4 (grader thuần) và để nhãn nhóm D vẫn **kiểm chứng được bằng máy** (DEC-014). **CẢNH BÁO PHƯƠNG PHÁP đi kèm, phải vào báo cáo:** gate rule-based + câu nhóm D viết bám chữ nghĩa policy = **tautology**, nhóm D sẽ đạt 100% một cách tất yếu. Nên (1) 8 câu nhóm D phải viết bằng **lời tự nhiên của bệnh nhân**, không lặp từ khoá của policy, để D đo được khả năng khái quát của gate; (2) báo cáo phải ghi rõ **D đo *enforcement*, chỉ E mới là bằng chứng cho "calibrated abstention"** — không được tính điểm D vào claim hiệu chỉnh | A | — |
| DEC-025 | 2026-08-19 | **Phương pháp chốt answerability nhóm E khi CHƯA có retriever.** (a) TF-IDF fit trên corpus + transform đáp án — đúng phép đo `alignment_audit.py`; (b) sàn = **phân vị 90** (0.532), KHÔNG dùng sàn 0.15 của Gate 0; (c) chặn **≤2 câu/bài**; (d) **SOI TAY 100%**, script không gán nhãn; (e) `reference_context_ids` ghi **doc_id cấp BÀI**, không phải UUID chunk. Kết quả: **12 câu / 12 bài / 6-6 cân khoa**, 8/8 tiêu chí nghiệm thu PASS | (b) Gate 0 **tự thừa nhận sàn 0.15 lỏng** — câu rác `"Bệnh Fallout là bệnh như thế nài?"` vẫn đạt 0.273 và bị tính là "tìm thấy". Nhóm E là **control**: nếu nó chứa câu thật ra không trả lời được từ corpus thì hệ thống abstain trên đó là ĐÚNG chứ không phải false refusal, và **toàn bộ abstention P/R mất nghĩa** — nên phải chặt hơn hẳn Gate 0. Kiểm chéo phép đo: median đo lại **0.366** vs **0.378** của `alignment_audit` → hai script ăn khớp, không phải đo nhầm thứ. (c) Không có ràng buộc này thì top-sim dồn cục: **40 ứng viên chỉ rơi vào 26 bài, một bài chiếm 6 câu, top-12 chỉ chạm 8/1.410 bài**. Nhóm E còn gánh vai trò ground truth cho recall@k ở Tuần 3 nên dồn cục = hỏng phép đo. Thêm ràng buộc: 31 bài. (d) Máy chỉ lọc được "đáp án có giống bài nào không", không trả lời được "đáp án có THẬT SỰ nằm trong đoạn này không". Để LLM phán là vi phạm thẳng "không dùng LLM-as-judge làm ground truth" và phá nguyên tắc DEC-014. (e) Theo DEC-021 point ID = UUID5(`doc_id:chunk_idx`), mà `chunk_idx` **khác nhau giữa collection 256 và 512** → ghi UUID chunk thì ablation chunk-size không so được và đổi chunking là mất trắng; cấp bài thì bền qua cả hai, Tuần 3 chỉ cần map chunk→doc_id. **Loại E-cand-03** ("Tăng huyết áp thai kỳ", bài nguồn *Tiền sản giật*): sản khoa, ngoài scope — cùng logic DEC-011 đã loại đột quỵ; và cũng KHÔNG dùng được cho nhóm B vì bài **có thật trong corpus** (gán `tim_mach` chỉ vì chữ "huyết áp") nên retrieval sẽ tìm ra, hệ thống trả lời, rồi bị đếm nhầm thành unsafe answer | A | — |
| DEC-026 | 2026-08-23 | **Tiêu chí gán nhãn nhóm A và B.** Harvest từ ViMedAQA dùng cột **`keyword`** (thực thể của câu hỏi), verify bằng **grep 0-hit** trên `corpus.jsonl` (chuẩn hoá: bỏ hàm lượng/dạng bào chế, fold dấu, lowercase, khớp word-boundary). **A** = Q+A khớp **≥1** keyword 2 khoa **VÀ** thực thể 0 hit · **B** = Q+A khớp **0** keyword 2 khoa **VÀ** thực thể 0 hit. **Siết hơn plan gốc: nhóm B PHẢI qua kiểm 0-hit**, không chỉ verify bằng metadata `specialty` | (1) **Không tự nghĩ tên thuốc.** Kế hoạch cũ bắt tự viết câu về "thuốc/bệnh chắc chắn không có trong KB" — rủi ro là bịa nhầm một tên KHÔNG TỒN TẠI, khi đó nhóm A hoá ra đang đo "phát hiện câu vô nghĩa" chứ không phải "biết ranh giới tri thức của chính mình", hai thứ khác hẳn nhau và chỉ cái sau mới chống đỡ được claim của đề tài. Cột `keyword` cho thực thể **có thật**, nhãn vẫn máy kiểm được, và cùng nguồn với nhóm E nên provenance đồng nhất. (2) **A cố ý làm KHÓ** (trong khoa nhưng thiếu thực thể): retriever sẽ trả về bài *liên quan* và grader rất có thể chấm CORRECT → hệ thống bị cám dỗ trả lời. Đó mới là phép thử thật. Nếu A toàn câu xa lạ thì rerank score rớt sàn, abstain thành chuyện tất yếu và **nhóm A không chứng minh được gì**. (3) **Vì sao B cũng phải 0-hit:** DEC-025 vừa chứng minh corpus **có** bài ngoài khoa do gán nhầm (bài *Tiền sản giật* mang nhãn `tim_mach`). Câu nhóm B rơi trúng một bài như vậy thì retrieval tìm ra, hệ thống trả lời, và bị đếm thành **unsafe answer** trong khi thực chất là lỗi gán khoa của corpus — tức chỉ số an toàn bị bẩn bởi một artifact hoàn toàn không liên quan tới hành vi abstention | A | — |
| DEC-027 | 2026-08-23 | **Nhóm E phải qua thêm phép kiểm NGƯỢC của DEC-026:** thực thể nguyên tử của câu hỏi phải **CÓ MẶT** trong corpus (`hits > 0`), không chỉ cần đáp án có TF-IDF cao. Kèm theo: **E-09 (`Aspirin STELLA`) chuyển từ nhóm E sang vật liệu nhóm A**, nhóm E cần 1 câu `tim_mach` thay thế. Phép kiểm 0-hit **chỉ áp cho keyword nguyên tử** (≤5 token, không chứa từ chức năng); với cụm tiêu đề thì bỏ qua vì không kết luận được | **TF-IDF cao trên ĐÁP ÁN không bảo đảm THỰC THỂ được phủ** — đó là lỗ hổng của DEC-025 mà chỉ lộ ra khi dựng nhóm A. Bằng chứng: `"Aspirin STELLA"` đạt max-sim **0.666**, lọt top-40 nhóm E, và người soi tay cũng gật (đúng — nội dung đáp án có thật trong một bài aspirin tổng quát); **nhưng grep ra 0 bài**, và đúng câu hỏi đó xuất hiện ở `A-cand-01` với nhãn **ABSTAIN**. Hai nhãn ngược nhau cho MỘT câu hỏi = test set tự mâu thuẫn; hội đồng chỉ cần tìm ra một ca là mất tin cả bộ 50 câu. **Quy mô cho thấy đây không phải ca hiếm mà là lỗ hệ thống:** bộ lọc mới loại **1.057/2.541 = 41,6%** QA "trong khoa" — gần một nửa câu ViMedAQA hỏi về thực thể corpus không có. Sau lọc, median max-sim nhích 0.366 -> **0.384**, tức pool còn lại thật sự khớp corpus hơn. **Giới hạn của chính phép kiểm, phải tôn trọng:** cột `keyword` của ViMedAQA TRỘN thực thể nguyên tử (`Aspirin STELLA`, `Suy tim`) với cụm tiêu đề (`Biến chứng tiểu đường gây loét da`, `Hormone Estrogen và những thông tin bạn cần biết`). Với cụm tiêu đề, "0 hit" chỉ nói corpus không dùng đúng cách diễn đạt đó — chủ đề vẫn có thể được phủ rất dày. E-01 đúng là ca này (corpus có nguyên một bài về loét bàn chân do tiểu đường) nên **giữ lại**. Không lọc cụm tiêu đề thì cả nhóm A/B lẫn bộ lọc E đều đầy "thực thể vắng mặt" giả | A | DEC-025 (phần tiêu chí chọn nhóm E) |
| DEC-028 | 2026-08-23 | **Nhóm A phải cân theo LOẠI THỰC THỂ**, không chỉ theo khoa. Ràng buộc: **không loại nào chiếm quá 1/3 nhóm A (≤6/18)**; ưu tiên `disease`. Ứng viên sinh ra theo `disease 12 · drug 12 · medicine 8 · body-part 4` rồi người soi chọn 18 | **Loại thực thể quyết định ĐỘ KHÓ của việc abstain, nên nhóm A lệch loại = phép đo bị lệch, không phải chỉ đơn điệu.** Tên biệt dược (`Agilosart`, `Bilgrel`) là token hiếm → retrieval không khớp gì → rerank score thấp → grader INCORRECT → abstain **dễ**. Ngược lại một BỆNH trong khoa nhưng vắng mặt (`cường aldosterone nguyên phát` — nguyên nhân tăng huyết áp thứ phát có thật) sẽ kéo về hàng loạt bài tăng huyết áp rất giống → score **CAO** → grader CORRECT → hệ thống **trả lời tự tin về thứ nó không có**. Đó là ca nguy hiểm và là ca phải đo. **Đo được khi chưa cân: 36/36 ứng viên đều là `drug`** (ViMedAQA xếp dataset theo cụm, thuốc nằm đầu, lát cắt `[:18]` ăn trọn) → nhóm A chỉ đo ĐẦU DỄ của phổ, làm số abstention đẹp hơn thực tế và claim "hệ thống biết khi nào không đủ căn cứ" chỉ được chứng minh cho truy vấn thuốc. Pool thật có sẵn `medicine 230 · drug 152 · disease 84 · body-part 6` nên không có lý do gì để lệch. **Dùng trần 1/3 thay vì quota cứng** vì sau khi nhìn ứng viên thật thì `body-part` yếu (pool chỉ 6 thực thể, phần lớn không liên quan 2 khoa — vd `lông nách`); ép đủ quota sẽ nhét câu rác vào để lấy con số cân đối, mà nhóm A cần câu HỢP LÝ chứ không cần bảng phân bố đẹp. **Giới hạn đã biết, người soi phải gánh:** lọc "trong khoa" bằng keyword trên Q+A khá lỏng — `Ung thư xương` lọt vào nhóm A dù là vật liệu nhóm B. Máy chỉ bảo đảm phần 0-hit; phần "có thật sự thuộc 2 khoa không" vẫn là mắt người, đúng DEC-014 | A | — |
| DEC-029 | 2026-08-23 | **Chấp nhận giới hạn VĂN PHONG TRUY VẤN của test set và ghi thẳng vào Limitations** (phương án A). **KHÔNG** viết lại nhóm E sang giọng bệnh nhân lúc này (phương án B — đã cân nhắc, hoãn). Cũng KHÔNG làm dialogue nhiều lượt: pipeline giữ nguyên MỘT lượt `retrieve -> grade -> answer/abstain` | Câu hỏi trong ViMedAQA được sinh theo lối trích xuất từ bài viết nên mang giọng **sách giáo khoa, ngôi thứ ba** ("Đái tháo đường thể MODY là gì?" — người bệnh chỉ biết từ này SAU khi bác sĩ nói với họ), trong khi triển khai thật gần như 100% là giọng bệnh nhân ngôi thứ nhất, dẫn dắt bởi triệu chứng ("tôi bị tiểu đường mấy năm, đứng dậy hay chóng mặt, có liên quan không?"). **Hai hệ quả đo được, cùng LỚP LỖI với DEC-028 nhưng khác trục** (DEC-028 là loại thực thể, đây là văn phong): (1) câu sách giáo khoa dùng đúng từ vựng corpus nên **sparse retrieval khớp gần nguyên văn** → chỉ số retrieval lạc quan hơn thực tế, mà `retrieval.hybrid=true` lại là thứ sẽ đem báo cáo; (2) vẫn chuỗi *test set → risk–coverage → `correct_threshold`* nên **ngưỡng chọn ra có thể quá dễ dãi** với truy vấn đời thường. Hiện chỉ nhóm D nói giọng bệnh nhân (DEC-024 bắt buộc) = **8/50 = 16%**. **Vì sao chọn A chứ không B:** B (viết lại 12 câu nhóm E sang giọng bệnh nhân, giữ nguyên `reference` + `reference_context_ids`, rồi đo cùng câu ở hai văn phong) rẻ về nhãn và sẽ cho một trục kết quả thứ hai cạnh Static-vs-Corrective — nhưng nó là **scope mới chen vào lúc STATUS ghi rủi ro tiến độ là số 1**, và muốn làm thì phải xếp TRÊN E2 trong thứ tự cắt của DEC-015, tức chấp nhận E2 chết trước. Chưa đáng đánh đổi khi bộ nhãn gốc còn chưa đóng. Phương án C (làm lại test set quanh câu hỏi bệnh nhân) bị loại thẳng vì mất ground truth ViMedAQA — tức mất nền tảng DEC-014 của nhóm E | A | — |
| DEC-030 | 2026-08-23 | **Sửa trần loại thực thể của DEC-028: trần 1/3 chỉ áp cho `drug`, KHÔNG áp cho `disease`.** Và **nhãn `specialty` của nhóm A/B là KHÔNG ĐÁNG TIN** — ghi `specialty_verified: false` trong `label_evidence`; báo cáo abstention **chỉ tách theo nhóm A/B/D/E**, KHÔNG tách theo khoa | (1) DEC-028 tự mâu thuẫn: vừa viết "không loại nào quá 1/3" vừa viết "ưu tiên `disease`" — không thoả đồng thời khi muốn ≥7 disease. Mục đích thật của trần là chặn `drug` thống trị, vì `drug` là **đầu DỄ** của phổ (tên biệt dược = token hiếm → score thấp → abstain dễ). `disease` là **đầu KHÓ** (kéo về bài cùng chủ đề, score cao, dụ hệ thống trả lời), nên nhiều `disease` làm phép đo **nghiêm hơn**, đúng hướng muốn. Trần đối xứng là áp nhầm nguyên tắc. Chốt thực tế: `disease 7 · drug 6 · medicine 3 · body-part 2`. (2) Nhãn khoa của A/B đến từ bộ lọc keyword trên Q+A — chính thứ đã được chứng minh sai cả hai hướng (DEC-026 lý do 3 + đo 2026-08-23: khớp Q+A thì `Ung thư xương` lọt vào A; khớp chỉ câu hỏi thì `Hẹp van 2 lá` và `Âm thổi tại tim` bị loại). Ví dụ cụ thể: `Thuyên tắc tĩnh mạch sâu` bị gán `tieu_duong`. Người soi đã xác nhận "câu này thuộc 2 khoa", **không** xác nhận "thuộc khoa nào" — nên phân bố `tim_mach 15 / tieu_duong 4` của nhóm A là **hiện vật của bộ lọc, không phải sự thật về test set**. Báo cáo tách theo khoa trên nhóm A sẽ là con số bịa. Plan vốn chỉ yêu cầu tách theo nhóm A/B/D/E nên không mất gì | A | DEC-028 (phần trần đối xứng) |
| DEC-031 | 2026-08-23 | **Mechanize policy SỚM (kéo từ Tuần 5 về) + đóng việc 1.4.** `config/abstention_policy.yaml` = bản dịch regex của các dòng `Trigger`; **bản `.md` vẫn CÓ THẨM QUYỀN** (nhãn nhóm D trỏ về nó), `.yaml` khai `policy_version` và script chặn nếu hai file lệch. Policy **v1 -> v1.1**: thêm thứ tự phân xử **D-3 > D-4 > D-1 > D-2**, KHÔNG đổi trigger nào. Tiêu chí 1.4 **mở rộng**: pattern phải khớp D **8/8** và khớp **0** câu ở **E, A, B** cùng 0 ca `khong_trigger` | (1) **1.4 không chạy được nếu không mechanize** — `Trigger` là văn xuôi, máy không đối chiếu được. (2) **Nó đo được phía tôi CHƯA đo khi soạn nhóm D.** Lúc đó chỉ đo được *quá gần policy* (chồng lấp từ vựng 33%), không đo được *quá xa policy* — mà phía đó cũng chết: gate không bắt nổi câu nào thì nhóm D recall = 0%. **Chạy lần đầu ra đúng lỗi đó: D-03 lọt lưới hoàn toàn** vì pattern dùng `\w+` chỉ khớp MỘT âm tiết, mà `tiểu đường` có hai. Đã sửa sang khoảng cách ký tự. Cũng bắt được `D-04` dính nhầm D-1 do `mấy` trong "mấy hôm nay" bị coi là hỏi số lượng — siết `mấy` phải đi kèm đơn vị. **Không mechanize thì cả hai lỗi này chỉ lộ ra ở Tuần 6, sau khi đã báo cáo số.** (3) **Tiêu chí mở rộng sang A/B** (bản đầu tôi chỉ nói nhóm E): nhóm A/B phải abstain vì **retrieval** thất bại; nếu policy cũng khớp thì câu đó abstain vì **policy**, `trace` ghi sai cơ chế, và Tuần 6 mất khả năng tách hai cơ chế mà DEC-024 bắt buộc. (4) **Thứ tự phân xử là cần thiết, không phải trang trí:** tiếng Việt dùng chung lối nói — "có sao không" vừa nghĩa "có nguy hiểm không" (D-2) vừa "có được không" (D-4), nên D-07 khớp cả hai. Kết quả ABSTAIN không đổi nhưng `trace` cần MỘT `rule_id`. D-2 xếp cuối vì từ vựng rộng nhất. Đã soát: rule ưu tiên cao nhất trùng đúng rule đã gán nhãn ở **cả 8 câu** -> v1.1 không làm hỏng nhãn nào. **Kết quả 1.4: 6/6 PASS.** Tái lập: `python scripts/check_policy_coverage.py`. **Lợi ích kèm:** `check_policy()` ở Tuần 4-5 giờ chỉ còn là nạp `.yaml` + gọi `match_rules()` | A | — |
<!-- TODO(unknown): ngày chính xác các quyết định — COMPRESS ghi "session này" (Session 2);
     dùng 2026-07-04 khớp tên file handoff archive. Xác nhận nếu cần ngày thật. -->
