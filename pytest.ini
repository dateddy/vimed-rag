[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
# -p no:cov: bỏ qua plugin pytest-cov nếu môi trường cài thiếu 'coverage'
# (scaffold không yêu cầu đo coverage). Xóa dòng này nếu bạn đã cài coverage.
addopts = -q -p no:cov
